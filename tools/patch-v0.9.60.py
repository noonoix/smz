#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""
patch-v0.9.60.py — Classroom Studio v0.9.60: firmware consolidation + transport hardening

اجرا از ریشه‌ی ریپو (یا با مسیر ریشه به‌عنوان آرگومان):

    python patch-v0.9.60.py [مسیر-ریشه-ریپو]

idempotent است؛ اجرای دوم همه‌جا «already patched» می‌گوید. قبل از نوشتن، EOL هر فایل
حفظ می‌شود و در پایان یک مرحله‌ی verify همه‌ی نشانه‌ها را چک می‌کند.
"""
import shutil
import sys
from pathlib import Path

ROOT = Path(sys.argv[1]).resolve() if len(sys.argv) > 1 else Path.cwd()
REPORT = []
FAILED = []


def _read(rel):
    p = ROOT / rel
    raw = p.read_bytes()
    text = raw.decode("utf-8")
    eol = "\r\n" if text.count("\r\n") > text.count("\n") / 2 else "\n"
    return text.replace("\r\n", "\n"), eol


def _write(rel, text, eol):
    p = ROOT / rel
    if eol == "\r\n":
        text = text.replace("\n", "\r\n")
    p.write_bytes(text.encode("utf-8"))


def edit(rel, mode, old, new, expect_min=1, note=""):
    try:
        text, eol = _read(rel)
    except FileNotFoundError:
        FAILED.append(f"MISSING FILE: {rel}")
        return
    # idempotency: applied when the new text is present AND the old anchor is either
    # gone or lives INSIDE the new text (prefix-shaped edits like inserted helpers/steps)
    if new in text and (old not in text or old in new):
        REPORT.append(f"already patched: {rel} {note}")
        return
    if old not in text:
        FAILED.append(f"ANCHOR NOT FOUND in {rel} {note} — first 80 chars: {old[:80]!r}")
        return
    n = text.count(old)
    if mode == "one" and n != 1:
        FAILED.append(f"ANCHOR NOT UNIQUE in {rel} {note}: found {n}")
        return
    if mode == "all" and n < expect_min:
        FAILED.append(f"TOO FEW anchor matches in {rel} {note}: found {n}, want >= {expect_min}")
        return
    text = text.replace(old, new) if mode == "all" else text.replace(old, new, 1)
    _write(rel, text, eol)
    REPORT.append(f"patched: {rel} {note} ({n} site(s))")


def swap_code_template(rel, region):
    start = '    private const string CodeTemplate = """'
    end = "\n        \"\"\";"
    text, eol = _read(rel)
    if "def pump_arm():" in text:   # template marker (NOT ERR|NOSENSOR — the README edit adds that string too)
        REPORT.append(f"already patched: {rel} (code template)")
        return
    if text.count(start) != 1:
        FAILED.append(f"CodeTemplate start marker not unique in {rel}")
        return
    i = text.index(start)
    j = text.find(end, i)
    if j < 0:
        FAILED.append(f"CodeTemplate end marker not found in {rel}")
        return
    text = text[:i] + region + text[j + len(end):]
    _write(rel, text, eol)
    REPORT.append(f"patched: {rel} (code template swapped)")


def write_file(rel, content):
    p = ROOT / rel
    p.parent.mkdir(parents=True, exist_ok=True)
    old = p.read_text(encoding="utf-8") if p.exists() else None
    if old == content:
        REPORT.append(f"already up to date: {rel}")
        return
    p.write_text(content, encoding="utf-8", newline="\n")
    REPORT.append(f"written: {rel}")


def append_file(rel, marker, block):
    p = ROOT / rel
    text = p.read_text(encoding="utf-8") if p.exists() else ""
    if marker in text:
        REPORT.append(f"already appended: {rel}")
        return
    if not text.endswith("\n"):
        text += "\n"
    p.write_text(text + block, encoding="utf-8")
    REPORT.append(f"appended: {rel}")


CODE_REGION = "    private const string CodeTemplate = \"\"\"\n        # Classroom Studio v__VERSION__ - Raspberry Pi Pico light-sensor firmware (CircuitPython)\n        # System: __MACHINE__   generated: __GENERATED__   calibrated states: __STATE_COUNT__\n        # Sensor: BH1750 (GY-302 / GY-30) on I2C0 - SDA=GP20, SCL=GP21, ADDR->GND => 0x23\n        # Board roles: the Pico is the executive brain - keyboard (USB HID) + light sensor + macro\n        # engine. Mouse and the sound sensor belong to the Arduino Pro Micro, which the Pico drives\n        # as its arm over UART0: GP16 = TX -> Pro Micro RX, GP17 = RX <- Pro Micro TX, common GND,\n        # 115200 8N1.\n        #\n        # v0.9.60 - hardened consolidation (bench-proven as the 0.9.59h/k/m/n line):\n        #   * BH1750 OPTIONAL: the brain boots and answers PING even with the sensor unplugged;\n        #     WLUX/TRGLUX/LCAL then answer ERR|NOSENSOR and armed light states stay silent.\n        #   * Serial RX is a bounded BYTE buffer - no per-read string concat, no heap churn.\n        #   * The Pro Micro arm is pumped EVERY loop iteration: its EVT| lines stream to the PC\n        #     live and its small TX buffer can never fill up and wedge the arm.\n        #   * Mouse commands are fire-and-ack (forwarded to the arm, OK answered at once) so dense\n        #     human paths stay smooth; sound/SETRES still wait for the arm's real reply.\n        #   * The keyboard ALWAYS runs on this Pico: KBDPICO| and the legacy arm keyboard envelope\n        #     are both consumed locally - the arm never types.\n        #   * Fixed keypad: GP4->GND = Num Lock = Start/Stop, GP3->GND = Scroll Lock = Pause/Resume.\n        #     Both drive the standalone engine AND send the HID key to the PC.\n        #   * Safe default: boot never starts the macro. The standalone engine waits for GP4\n        #     (Num Lock) unless AUTOSTART below is explicitly True, and stays silent for 3 s after\n        #     the last host command so host-driven runs never double-fire the armed states.\n        #\n        # Copy code.py + boot.py + pico-calibration.json onto CIRCUITPY and put the\n        # adafruit_hid package into /lib. Full instructions: README-FLASH.md\n\n        import time\n        import random\n        import json\n        import board\n        import busio\n        import usb_cdc\n        import usb_hid\n        from adafruit_hid.keyboard import Keyboard\n        from adafruit_hid.keycode import Keycode\n\n        ADDR = 0x23\n        POWER_ON = 0x01\n        RESET = 0x07\n        CONT_HIRES = 0x10   # 1 lux, ~120 ms per sample (max 180 ms)\n        CONT_LORES = 0x13   # 4 lux, ~16 ms per sample\n\n        # Play Options baked at export time: how often the armed standalone states run.\n        LOOP_MODE = \"__LOOP_MODE__\"      # once | times | timed | forever\n        LOOP_COUNT = __LOOP_COUNT__      # passes for \"times\"\n        LOOP_SECONDS = __LOOP_SECONDS__  # seconds for \"timed\"\n\n        # v0.9.60 - safe default: copying/booting code.py NEVER starts the macro by itself.\n        # Set True only for a deliberate power-on-autorun scenario.\n        AUTOSTART = False\n\n        SPECIAL_VK = {\n            0x0D: Keycode.ENTER, 0x1B: Keycode.ESCAPE, 0x20: Keycode.SPACE, 0x09: Keycode.TAB,\n            0x08: Keycode.BACKSPACE, 0x25: Keycode.LEFT_ARROW, 0x27: Keycode.RIGHT_ARROW,\n            0x26: Keycode.UP_ARROW, 0x28: Keycode.DOWN_ARROW,\n        }\n        # modifiers too, so KCOMBO shortcuts (Ctrl/Shift/Alt/Win + key) work on the Pico\n        MOD_VK = {\n            0xA0: Keycode.LEFT_SHIFT, 0xA1: Keycode.RIGHT_SHIFT,\n            0xA2: Keycode.LEFT_CONTROL, 0xA3: Keycode.RIGHT_CONTROL,\n            0xA4: Keycode.LEFT_ALT, 0xA5: Keycode.RIGHT_ALT,\n            0x5B: Keycode.LEFT_GUI, 0x5C: Keycode.RIGHT_GUI,\n        }\n        DIGITS = (\"ZERO\", \"ONE\", \"TWO\", \"THREE\", \"FOUR\", \"FIVE\", \"SIX\", \"SEVEN\", \"EIGHT\", \"NINE\")\n\n\n        def keycode_for_vk(vk):\n            if 0x41 <= vk <= 0x5A:\n                return getattr(Keycode, chr(vk))\n            if 0x30 <= vk <= 0x39:\n                return getattr(Keycode, DIGITS[vk - 0x30])\n            if 0x70 <= vk <= 0x7B:\n                return getattr(Keycode, \"F\" + str(vk - 0x6F))\n            return SPECIAL_VK.get(vk, MOD_VK.get(vk, Keycode.E))   # modifiers included\n\n\n        class Bh1750:\n            def __init__(self, i2c, mode=CONT_HIRES):\n                self._i2c = i2c\n                self._buf = bytearray(2)\n                self.mode = None\n                self._cmd(POWER_ON)\n                self._cmd(RESET)\n                self.set_mode(mode)\n\n            def _cmd(self, value):\n                while not self._i2c.try_lock():\n                    pass\n                try:\n                    self._i2c.writeto(ADDR, bytes([value]))\n                finally:\n                    self._i2c.unlock()\n\n            def set_mode(self, mode):\n                if mode == self.mode:\n                    return\n                self.mode = mode\n                self._cmd(mode)          # sent once per mode change, never per sample\n                time.sleep(0.2 if mode == CONT_HIRES else 0.05)\n\n            def lux(self):\n                while not self._i2c.try_lock():\n                    pass\n                try:\n                    self._i2c.readfrom_into(ADDR, self._buf)\n                finally:\n                    self._i2c.unlock()\n                raw = (self._buf[0] << 8) | self._buf[1]\n                return raw / 1.2         # datasheet conversion\n\n\n        def median(values):\n            ordered = sorted(values)\n            return ordered[len(ordered) // 2]\n\n\n        def load_states():\n            try:\n                with open(\"/pico-calibration.json\", \"r\") as fh:\n                    return json.load(fh).get(\"states\", [])\n            except Exception:\n                return []\n\n\n        kbd = Keyboard(usb_hid.devices)\n        serial = usb_cdc.data if usb_cdc.data is not None else usb_cdc.console\n        states = load_states()\n        window = []\n\n        # v0.9.60 - the light sensor is OPTIONAL. A loose SDA/SCL wire must never kill the brain\n        # before its command loop (that was the silent boot death): without the sensor the light\n        # commands answer ERR|NOSENSOR and everything else keeps working.\n        try:\n            i2c = busio.I2C(board.GP21, board.GP20)   # SCL, SDA\n            sensor = Bh1750(i2c)\n        except Exception:\n            sensor = None\n\n        # Commands that are not the brain's own job: forwarded to the Pro Micro arm.\n        ARM_PREFIXES = (\"MMOVE\", \"MCLICK\", \"MWHEEL\", \"MDOWN\", \"MUP\", \"SETRES\", \"WSND\", \"TRGSND\", \"SCAL\")\n        # v0.9.60 - mouse goes fire-and-ack (smooth dense paths); the rest waits for the arm reply.\n        MOUSE_PREFIXES = (\"MMOVE\", \"MCLICK\", \"MWHEEL\", \"MDOWN\", \"MUP\")\n        # Keyboard commands: ALWAYS typed locally by this Pico (final contract).\n        KBD_PREFIXES = (\"KTEXT\", \"KCOMBO\", \"KDOWN\", \"KUP\")\n\n        try:\n            arm = busio.UART(board.GP16, board.GP17, baudrate=115200, timeout=0.2)\n        except Exception:\n            arm = None\n\n        # v0.9.60 - fixed keypad (contract): GP4->GND = Num Lock = Start/Stop,\n        # GP3->GND = Scroll Lock = Pause/Resume. Active-low with pull-ups, edge-detected.\n        try:\n            import digitalio\n            btn1 = digitalio.DigitalInOut(board.GP4)\n            btn1.direction = digitalio.Direction.INPUT\n            btn1.pull = digitalio.Pull.UP\n            btn2 = digitalio.DigitalInOut(board.GP3)\n            btn2.direction = digitalio.Direction.INPUT\n            btn2.pull = digitalio.Pull.UP\n            _last_btn1 = True\n            _last_btn2 = True\n        except Exception:\n            btn1 = btn2 = None\n            _last_btn1 = _last_btn2 = True\n\n        engine_on = AUTOSTART        # GP4 toggles this (Num Lock = Start/Stop)\n        engine_paused = False        # GP3 toggles this (Scroll Lock = Pause/Resume)\n        last_host_cmd = time.monotonic()\n\n\n        def _serial_write_line(text):\n            try:\n                serial.write((text + \"\\n\").encode(\"utf-8\"))\n            except Exception:\n                pass\n\n\n        _arm_buf = bytearray()\n\n\n        def pump_arm():\n            \"\"\"v0.9.60 - permanent arm pump. Called on EVERY loop iteration (and inside every\n            blocking wait) so the Pro Micro's small TX buffer can never fill up and wedge it.\n            Arm EVT| lines stream to the PC live; fire-acked mouse OKs are discarded; every\n            other reply is returned for a waiting forward_to_arm.\"\"\"\n            if arm is None:\n                return []\n            try:\n                n = arm.in_waiting\n                if n:\n                    _arm_buf.extend(arm.read(n))\n                    if len(_arm_buf) > 1024:              # runaway-garbage guard\n                        del _arm_buf[:-256]\n            except Exception:\n                return []\n            ready = []\n            while True:\n                nl = _arm_buf.find(b\"\\n\")\n                if nl < 0:\n                    break\n                raw = bytes(_arm_buf[:nl])\n                del _arm_buf[:nl + 1]\n                line = raw.decode(\"utf-8\", \"replace\").strip()\n                if not line:\n                    continue\n                if line.startswith(\"EVT|\"):\n                    _serial_write_line(line)              # arm events reach the PC live\n                    continue\n                parts = line.split(\"|\")\n                if len(parts) > 1 and parts[0] == \"OK\" and parts[1] in MOUSE_PREFIXES:\n                    continue                              # the PC already got its fire-and-ack\n                ready.append(line)\n            return ready\n\n\n        def _arm_write(line):\n            if arm is None:\n                return False\n            try:\n                arm.write((line + \"\\n\").encode(\"utf-8\"))\n                return True\n            except Exception:\n                return False\n\n\n        def forward_fast(line):\n            \"\"\"v0.9.60 - mouse fast path: forward to the arm and answer the PC at once; the\n            pump later collects and discards the arm's own OK.\"\"\"\n            head = line.split(\"|\")[0]\n            if not _arm_write(line):\n                return \"ERR|NOARM|\" + head\n            return \"OK|\" + head\n\n\n        def forward_to_arm(line, timeout_s):\n            \"\"\"Blocking forward for commands whose reply the PC needs (sound, SETRES, HALT/BYE).\n            Keeps pumping while waiting so arm EVT| lines still stream to the PC.\"\"\"\n            head = line.split(\"|\")[0]\n            if not _arm_write(line):\n                return \"ERR|NOARM|\" + head\n            end = time.monotonic() + timeout_s\n            while time.monotonic() < end:\n                for reply in pump_arm():\n                    # commands are strictly serialized and mouse OKs are already filtered:\n                    # the first non-mouse OK/ERR we see belongs to this command.\n                    if reply.split(\"|\")[0] in (\"OK\", \"ERR\"):\n                        return reply\n                time.sleep(0.005)\n            return \"ERR|TIMEOUT|\" + head\n\n\n        def sample():\n            window.append(sensor.lux())\n            if len(window) > 5:\n                window.pop(0)\n            return median(window)\n\n\n        def press(vk, hold_ms):\n            code = keycode_for_vk(vk)\n            kbd.press(code)\n            time.sleep(max(10, hold_ms) / 1000)\n            kbd.release(code)\n\n\n        def tap_key(code):\n            \"\"\"v0.9.60 - one fixed keypad tap (Num Lock / Scroll Lock) towards the PC.\"\"\"\n            try:\n                kbd.press(code)\n                time.sleep(0.04)\n                kbd.release(code)\n            except Exception:\n                pass\n\n\n        def poll_keypad():\n            \"\"\"v0.9.60 - fixed mapping, independent of the app's Options: GP4 toggles the\n            standalone engine (and sends Num Lock), GP3 toggles pause (and sends Scroll Lock).\"\"\"\n            global engine_on, engine_paused, _last_btn1, _last_btn2\n            if btn1 is None:\n                return\n            b1 = not btn1.value\n            b2 = not btn2.value\n            if b1 and not _last_btn1:\n                engine_on = not engine_on\n                if not engine_on:\n                    engine_paused = False\n                tap_key(Keycode.KEYPAD_NUMLOCK)\n            if b2 and not _last_btn2:\n                engine_paused = not engine_paused\n                tap_key(Keycode.SCROLL_LOCK)\n            _last_btn1 = b1\n            _last_btn2 = b2\n\n\n        def wait_range(lo, hi, stable_ms, timeout_ms, mode):\n            sensor.set_mode(CONT_LORES if mode == 1 else CONT_HIRES)\n            deadline = time.monotonic() + timeout_ms / 1000\n            inside_since = None\n            while time.monotonic() < deadline:\n                pump_arm()                            # v0.9.60 - the arm is drained even mid-wait\n                value = sample()\n                if lo <= value <= hi:\n                    if inside_since is None:\n                        inside_since = time.monotonic()\n                    elif (time.monotonic() - inside_since) * 1000 >= stable_ms:\n                        return value      # stabilized inside the range for n seconds\n                else:\n                    inside_since = None\n                time.sleep(0.02)\n            return None\n\n\n        def ints(fields, count, default=0):\n            out = []\n            for i in range(count):\n                try:\n                    out.append(int(fields[i]))\n                except Exception:\n                    out.append(default)\n            return out\n\n\n        def handle_keyboard(line, head):\n            # The Pico owns the keyboard (final contract) - always local execution.\n            if head == \"KDOWN\":\n                kbd.press(keycode_for_vk(int(line.split(\"|\")[1])))\n                return \"OK|KDOWN\"\n            if head == \"KUP\":\n                kbd.release(keycode_for_vk(int(line.split(\"|\")[1])))\n                return \"OK|KUP\"\n            if head == \"KCOMBO\":\n                # KCOMBO|vk+vk+...[,hmin,hmax] - press in order, random hold, release in reverse\n                body = line.split(\"|\", 1)[1]\n                hold = 0.0\n                if \",\" in body:\n                    body, holdpart = body.split(\",\", 1)\n                    hp = holdpart.split(\",\")\n                    try:\n                        h0 = int(hp[0])\n                        h1 = int(hp[1]) if len(hp) > 1 else h0\n                        hold = random.uniform(max(0, h0), max(0, h1)) if max(h0, h1) > 0 else 0.0\n                    except Exception:\n                        hold = 0.0\n                codes = [keycode_for_vk(int(v)) for v in body.split(\"+\") if v]\n                for c in codes:\n                    kbd.press(c)\n                time.sleep(max(10.0, hold) / 1000)\n                for c in reversed(codes):\n                    kbd.release(c)\n                return \"OK|KCOMBO\"\n            if head == \"KTEXT\":\n                # KTEXT|hmin,hmax,text - ASCII only, per-key random delay\n                parts = line.split(\"|\", 1)[1].split(\",\", 2)\n                try:\n                    hmin, hmax = int(parts[0]), int(parts[1])\n                except Exception:\n                    hmin, hmax = 0, 0\n                txt = parts[2] if len(parts) > 2 else \"\"\n                for ch in txt:\n                    if ord(ch) < 32 or ord(ch) > 126:\n                        return \"ERR|ASCII|KTEXT\"\n                for ch in txt:\n                    kbd.write(ch)\n                    if hmax > 0:\n                        time.sleep(random.uniform(max(0, hmin), hmax) / 1000)\n                return \"OK|KTEXT\"\n            return \"ERR|UNKNOWN|\" + head\n\n\n        def handle(line):\n            if line == \"PING\":\n                return \"OK|PONG|pico-light __VERSION__|role=brain+keyboard+light|arm=promicro\"\n            if line.startswith(\"LCAL|\"):\n                if sensor is None:\n                    return \"ERR|NOSENSOR|LCAL\"\n                ms = ints(line.split(\"|\")[1].split(\",\"), 1, 2000)[0] or 2000\n                sensor.set_mode(CONT_HIRES)\n                end = time.monotonic() + ms / 1000\n                readings = []\n                while time.monotonic() < end:\n                    pump_arm()                        # v0.9.60 - the arm is drained even mid-calibration\n                    readings.append(sensor.lux())\n                    time.sleep(0.02)\n                if not readings:\n                    readings = [sensor.lux()]\n                lo = int(min(readings))\n                hi = int(max(readings))\n                avg = int(sum(readings) / len(readings))\n                return \"OK|LCAL|min=%d|max=%d|avg=%d\" % (lo, hi, avg)\n            if line.startswith(\"WLUX|\"):\n                if sensor is None:\n                    return \"ERR|NOSENSOR|WLUX\"\n                a = ints(line.split(\"|\")[1].split(\",\"), 5)\n                got = wait_range(a[0], a[1], a[2] or 2000, a[3] or 20000, a[4])\n                if got is None:\n                    return \"ERR|TIMEOUT|WLUX\"\n                return \"OK|WLUX|lux=%d\" % int(got)\n            if line.startswith(\"TRGLUX|\"):\n                if sensor is None:\n                    return \"ERR|NOSENSOR|TRGLUX\"\n                a = ints(line.split(\"|\")[1].split(\",\"), 10)\n                got = wait_range(a[0], a[1], a[2] or 2000, a[3] or 20000, a[4])\n                if got is None:\n                    return \"ERR|TIMEOUT|TRGLUX\"\n                time.sleep(max(0, a[6]) / 1000)   # reaction delay (reactMin)\n                press(a[5], a[8] or 40)           # board-side keypress (holdMin)\n                return \"EVT|TRGLUX|lux=%d|vk=%d\" % (int(got), a[5])\n            if line in (\"HALT\", \"BYE\"):\n                if arm is not None:\n                    forward_to_arm(line, 2)          # stop the arm too\n                return \"OK|\" + line\n            # v0.9.60 - final contract: the Pico ALWAYS owns the keyboard. Both envelopes are\n            # consumed locally; the legacy arm envelope is accepted for backward compatibility\n            # but is never forwarded (the Pro Micro never types).\n            if line.startswith(\"KBDPICO|\"):\n                line = line.split(\"|\", 1)[1]\n            elif line.startswith(\"KBDARM|\"):\n                line = line.split(\"|\", 1)[1]\n            head = line.split(\"|\")[0]\n            if head in KBD_PREFIXES:\n                return handle_keyboard(line, head)\n            if head in MOUSE_PREFIXES:\n                return forward_fast(line)\n            if head in ARM_PREFIXES:\n                tmo = 30 if head in (\"WSND\", \"TRGSND\", \"SCAL\") else 5\n                return forward_to_arm(line, tmo)\n            return \"ERR|UNKNOWN|\" + line\n\n\n        def standalone_pass():\n            # No host command pending: run the armed states of this system in order.\n            if sensor is None:\n                return                                # v0.9.60 - light states stay silent without a sensor\n            for st in states:\n                if not st.get(\"armed\"):\n                    continue\n                got = wait_range(st.get(\"luxLow\", 0), st.get(\"luxHigh\", 0),\n                                 st.get(\"stableMs\", 2000), st.get(\"timeoutMs\", 20000),\n                                 st.get(\"mode\", 0))\n                if got is not None:\n                    press(st.get(\"keyVk\", 69), 40)\n\n\n        def loop_due():\n            # Play Options baked at export: how often the armed states run standalone\n            if LOOP_MODE == \"once\":\n                return passes == 0\n            if LOOP_MODE == \"times\":\n                return passes < LOOP_COUNT\n            if LOOP_MODE == \"timed\":\n                return time.monotonic() - started < LOOP_SECONDS\n            return True                      # forever\n\n\n        buffer = bytearray()               # v0.9.60 - bounded byte buffer, not string concat\n        passes = 0\n        started = time.monotonic()\n        while True:\n            try:\n                pump_arm()                            # v0.9.60 - the arm is drained EVERY iteration\n                if serial is not None and serial.in_waiting:\n                    buffer.extend(serial.read(serial.in_waiting))\n                    if len(buffer) > 4096:            # runaway guard: keep the newest 1 KB\n                        del buffer[:-1024]\n                    while True:\n                        nl = buffer.find(b\"\\n\")\n                        if nl < 0:\n                            break\n                        raw = bytes(buffer[:nl])\n                        del buffer[:nl + 1]\n                        line = raw.decode(\"utf-8\", \"replace\").strip()\n                        if not line:\n                            continue\n                        last_host_cmd = time.monotonic()\n                        try:\n                            _serial_write_line(handle(line))\n                        except Exception:\n                            _serial_write_line(\"ERR|EXC|\" + line.split(\"|\")[0])\n                else:\n                    poll_keypad()\n                    host_quiet = time.monotonic() - last_host_cmd >= 3   # no double-fire after host runs\n                    if engine_on and not engine_paused and host_quiet and states and loop_due():\n                        standalone_pass()\n                        passes += 1\n                    time.sleep(0.02)\n            except Exception:\n                # v0.9.60 - never-die: a bad line or a transient USB hiccup must never kill\n                # code.py. (KeyboardInterrupt/Ctrl+C still stops it - it is not an Exception.)\n                try:\n                    time.sleep(0.05)\n                except Exception:\n                    pass\n        \"\"\";"
EDITS = [
    ("ams-shell/bridge/bridge.py", "one", "PRINT_LOCK = threading.Lock()\n\n\ndef emit(obj):\n    with PRINT_LOCK:\n        sys.stdout.write(json.dumps(obj, ensure_ascii=False) + \"\\n\")\n        sys.stdout.flush()", "PRINT_LOCK = threading.Lock()\n\n# v0.9.60 — never-die emit: force UTF-8 on the stdio trio regardless of the Windows\n# console code page. Before this, REPORTING an error that contained non-cp1252 text\n# (Persian messages, Windows error strings) raised UnicodeEncodeError and killed the\n# whole bridge, hiding the real error behind a generic \"connect failed: timed out\".\nfor _stream in (sys.stdout, sys.stderr, sys.stdin):\n    try:\n        _stream.reconfigure(encoding=\"utf-8\", errors=\"replace\")\n    except Exception:\n        pass\n\n\ndef emit(obj):\n    try:\n        line = json.dumps(obj, ensure_ascii=False)\n    except Exception:\n        line = json.dumps(obj, ensure_ascii=True)\n    with PRINT_LOCK:\n        try:\n            sys.stdout.write(line + \"\\n\")\n        except UnicodeEncodeError:\n            sys.stdout.write(json.dumps(obj, ensure_ascii=True) + \"\\n\")\n        except Exception:\n            return\n        try:\n            sys.stdout.flush()\n        except Exception:\n            pass", 1),
    ("ams-shell/src/Ams.UI/Services/PicoFirmwareExporter.cs", "one", "    public const string BundleVersion = \"0.9.58\";   // UART arm moved to GP16/GP17 (wiring v6)", "    public const string BundleVersion = \"0.9.60\";   // v0.9.60 — hardened consolidation: optional sensor, byte buffer, arm pump, fixed keypad, Pico-only keyboard", 1),
    ("ams-shell/src/Ams.UI/Services/PicoFirmwareExporter.cs", "one", "            .Replace(\"__LOOP_SECONDS__\", loopSeconds.ToString(CultureInfo.InvariantCulture))\n            .Replace(\"__KBD_ON_ARM__\", keyboardOnArm ? \"True\" : \"False\")\n            .Replace(\"__RUNSTOP_HOTKEY__\", HotkeyTuple(runStopHotkey))\n            .Replace(\"__PAUSERESUME_HOTKEY__\", HotkeyTuple(pauseResumeHotkey));", "            .Replace(\"__LOOP_SECONDS__\", loopSeconds.ToString(CultureInfo.InvariantCulture));\n        // v0.9.60 — final hardware contract: the keyboard always runs on the Pico and the\n        // keypad is fixed (GP4 = Num Lock start/stop, GP3 = Scroll Lock pause/resume), so\n        // keyboardOnArm and the Options gestures no longer reach the firmware. The\n        // parameters stay for call-site compatibility.", 1),
    ("ams-shell/src/Ams.UI/Services/PicoFirmwareExporter.cs", "one", "        sb.AppendLine(\"Play Options baked in (v0.9.44): loop=\" + loopMode + (loopMode == \"times\" ? \" x\" + loopCount : loopMode == \"timed\" ? \" \" + loopSeconds + \"s\" : \"\") + \" | keyboard=\" + (keyboardOnArm ? \"Pro Micro (arm)\" : \"Pico (brain)\"));   // v0.9.44", "        sb.AppendLine(\"Play Options baked in: loop=\" + loopMode + (loopMode == \"times\" ? \" x\" + loopCount : loopMode == \"timed\" ? \" \" + loopSeconds + \"s\" : \"\") + \" | keyboard=Pico (fixed v0.9.60 contract) | keypad GP4=NumLock start/stop, GP3=ScrollLock pause/resume | AUTOSTART=False\");", 1),
    ("ams-shell/src/Ams.UI/Services/PicoFirmwareExporter.cs", "one", "        sb.AppendLine(\"- `HALT` / `BYE` -> `OK|...`\");\n        return sb.ToString();", "        sb.AppendLine(\"- `HALT` / `BYE` -> `OK|...`\");\n        sb.AppendLine(\"- v0.9.60: without the BH1750 wired, WLUX/TRGLUX/LCAL answer `ERR|NOSENSOR|...` and the brain stays alive (keyboard + arm keep working)\");\n        sb.AppendLine(\"- v0.9.60: mouse commands are fire-and-ack for smooth dense paths; arm events (EVT|) stream to the PC live\");\n        return sb.ToString();", 1),
    ("ams-shell/src/Ams.UI/Services/PythonBoardBridge.cs", "one", "            UseShellExecute = false,\n            CreateNoWindow = true,\n        };\n        psi.ArgumentList.Add(_bridgeScript);", "            UseShellExecute = false,\n            CreateNoWindow = true,\n            // v0.9.60 — read the bridge as UTF-8 (Persian log/error lines arrive intact;\n            // pairs with bridge.py's stdout reconfigure + ensure_ascii fallback)\n            StandardOutputEncoding = Encoding.UTF8,\n            StandardErrorEncoding = Encoding.UTF8,\n        };\n        psi.EnvironmentVariables[\"PYTHONIOENCODING\"] = \"utf-8\";   // v0.9.60 — python's own stderr prints too\n        psi.ArgumentList.Add(_bridgeScript);", 1),
    ("ams-shell/src/Ams.UI/MainWindow.xaml", "one", "                <ListBox x:Name=\"SerialLogList\" ItemsSource=\"{Binding LogLines}\" Height=\"160\"\n                         Background=\"Transparent\" BorderThickness=\"0\"", "                <ListBox x:Name=\"SerialLogList\" ItemsSource=\"{Binding LogLines}\" Height=\"160\"\n                         SelectionMode=\"Extended\"\n                         Background=\"Transparent\" BorderThickness=\"0\"", 1),
    ("ams-shell/src/Ams.UI/Views/StepDialog.xaml.cs", "one", "using System.Windows;\nusing System.Windows.Controls;\nusing System.Windows.Media;\nusing Ams.UI.Models;", "using System.Globalization;\nusing System.Windows;\nusing System.Windows.Controls;\nusing System.Windows.Media;\nusing Ams.UI.Models;", 1),
    ("ams-shell/src/Ams.UI/Views/StepDialog.xaml.cs", "one", "            if (f.Kind == FieldKind.Float &&\n                !double.TryParse(((Wpf.Ui.Controls.TextBox)_controls[f.Key]).Text, out _))", "            if (f.Kind == FieldKind.Float &&\n                !TryParseFloat(((Wpf.Ui.Controls.TextBox)_controls[f.Key]).Text, out _))", 1),
    ("ams-shell/src/Ams.UI/Views/StepDialog.xaml.cs", "one", "                FieldKind.Float => double.Parse(((Wpf.Ui.Controls.TextBox)_controls[f.Key]).Text),", "                FieldKind.Float => ParseFloat(((Wpf.Ui.Controls.TextBox)_controls[f.Key]).Text),", 1),
    ("ams-shell/src/Ams.UI/Views/StepDialog.xaml.cs", "one", "    private static string PropAsString(object? v) => v switch", "    // v0.9.60 — fractional fields accept both \"0.5\" (invariant) and the local decimal\n    // separator, regardless of the Windows display language.\n    private static bool TryParseFloat(string s, out double v)\n        => double.TryParse(s, NumberStyles.Float, CultureInfo.InvariantCulture, out v)\n           || double.TryParse(s, NumberStyles.Float, CultureInfo.CurrentCulture, out v);\n\n    private static double ParseFloat(string s)\n        => double.TryParse(s, NumberStyles.Float, CultureInfo.InvariantCulture, out var v) ? v\n           : double.Parse(s, NumberStyles.Float, CultureInfo.CurrentCulture);\n\n    private static string PropAsString(object? v) => v switch", 1),
    ("ams-shell/src/Ams.UI/Models/StepDefinitions.cs", "one", "using System;\nusing System.Text.RegularExpressions;", "using System;\nusing System.Globalization;\nusing System.Text.RegularExpressions;", 1),
    ("ams-shell/src/Ams.UI/Models/StepDefinitions.cs", "one", "    private static int LuxVk(StepNode s) => KeyMap.VK.TryGetValue(PropEx.GetString(s.Props, \"key\", \"E\"), out int vk) ? vk : 69;", "    private static int LuxVk(StepNode s) => KeyMap.VK.TryGetValue(PropEx.GetString(s.Props, \"key\", \"E\"), out int vk) ? vk : 69;\n    /// <summary>v0.9.60 — stableSec is fractional (FieldKind.Float): the summary must show\n    /// \"0.5s\", not a rounded-down integer. Invariant \"0.#\" keeps the dot in every locale.</summary>\n    private static string StableSecText(StepNode s)\n        => PropEx.GetDouble(s.Props, \"stableSec\", 2).ToString(\"0.#\", CultureInfo.InvariantCulture);", 1),
    ("ams-shell/src/Ams.UI/Models/StepDefinitions.cs", "all", "PropEx.GetInt(s.Props, \"stableSec\", 2)", "StableSecText(s)", 2),
    ("ams-shell/src/Ams.UI/ViewModels/MainViewModel.cs", "all", "Classroom Studio v0.9.58", "Classroom Studio v0.9.60", 1),
    ("ams-shell/src/Ams.UI/Ams.UI.csproj", "one", "<Version>0.9.58</Version>", "<Version>0.9.60</Version>", 1),
    ("tests/TestRunner.cs", "all", "<Version>0.9.58</Version>", "<Version>0.9.60</Version>", 20),
    ("tests/TestRunner.cs", "all", "Classroom Studio v0.9.58", "Classroom Studio v0.9.60", 8),
    ("tests/TestRunner.cs", "all", "BundleVersion = \\\"0.9.58\\\"", "BundleVersion = \\\"0.9.60\\\"", 8),
    ("tests/TestRunner.cs", "one", "PicoFirmwareExporter.BundleVersion == \"0.9.58\"", "PicoFirmwareExporter.BundleVersion == \"0.9.60\"", 1),
    ("tests/TestRunner.cs", "all", "BundleVersion is 0.9.58", "BundleVersion is 0.9.60", 1),
    ("tests/TestRunner.cs", "all", "csproj version is 0.9.58", "csproj version is 0.9.60", 2),
    ("tests/TestRunner.cs", "one", "var curMinor = 58;", "var curMinor = 60;", 1),
    ("tests/TestRunner.cs", "one", "        var v44arm = PicoFirmwareExporter.BuildCodePy(new List<PicoFirmwareExporter.LightState>(), \"M\", \"forever\", 0, 0, true);\n        Assert(v44arm.Contains(\"KBD_ON_ARM = True\") && v44arm.Contains(\"KBD_PREFIXES\"),\n            \"v0.9.44: with the arm chosen, code.py forwards keyboard commands over UART\");\n        var v44pico = PicoFirmwareExporter.BuildCodePy(new List<PicoFirmwareExporter.LightState>(), \"M\", \"forever\", 0, 0, false);\n        Assert(v44pico.Contains(\"KBD_ON_ARM = False\") && v44pico.Contains(\"def handle_keyboard(\"),\n            \"v0.9.44: with the Pico chosen, code.py types locally (KTEXT/KCOMBO/KDOWN/KUP handled)\");", "        // v0.9.60 — final contract: the keyboard ALWAYS runs on the Pico; the legacy\n        // keyboard-board option no longer reaches the firmware (it stays an app-side default only).\n        var v44arm = PicoFirmwareExporter.BuildCodePy(new List<PicoFirmwareExporter.LightState>(), \"M\", \"forever\", 0, 0, true);\n        var v44pico = PicoFirmwareExporter.BuildCodePy(new List<PicoFirmwareExporter.LightState>(), \"M\", \"forever\", 0, 0, false);\n        Assert(!v44arm.Contains(\"KBD_ON_ARM\") && !v44pico.Contains(\"KBD_ON_ARM\")\n               && v44arm.Contains(\"KBD_PREFIXES\") && v44pico.Contains(\"def handle_keyboard(\"),\n            \"v0.9.60: code.py always types locally on the Pico, whatever the legacy keyboard-board option says\");", 1),
    ("tests/TestRunner.cs", "one", "        // (b) v0.9.58d: keypad emits the two gestures saved in Options (no fixed lock keys)\n        var p58exp = V27ReadSrc(Path.Combine(\"Services\", \"PicoFirmwareExporter.cs\"));\n        var p58keys = PicoFirmwareExporter.HotkeyToVirtualKeys(\"Ctrl+Add\");\n        Assert(p58keys.SequenceEqual(new[] { 0xA2, 0x6B }),\n            \"v0.9.58d: Pico exporter converts configured modifier + numpad key\");\n        var p58code = PicoFirmwareExporter.BuildCodePy(new List<PicoFirmwareExporter.LightState>(), \"M\",\n            runStopHotkey: \"Shift+F7\", pauseResumeHotkey: \"Ctrl+Add\");\n        Assert(p58code.Contains(\"RUNSTOP_HOTKEY = (160, 118)\")\n               && p58code.Contains(\"PAUSERESUME_HOTKEY = (162, 107)\"),\n            \"v0.9.58d: exported code.py bakes the actual Options gestures\");\n        Assert(p58exp.Contains(\"send_hotkey(RUNSTOP_HOTKEY)\")\n               && p58exp.Contains(\"send_hotkey(PAUSERESUME_HOTKEY)\")\n               && !p58exp.Contains(\"kbd.send(Keycode.NUM_LOCK)\"),\n            \"v0.9.58d: Pico buttons no longer emit fixed Num/Scroll Lock keys\");\n        Assert(p58exp.Contains(\"btn1\") && p58exp.Contains(\"digitalio.DigitalInOut(board.GP2)\"),\n            \"v0.9.58: keypad button reader on GP2 with pull-up\");", "        // (b) v0.9.60: FIXED keypad contract — GP4 = Num Lock (start/stop), GP3 = Scroll Lock\n        // (pause/resume), independent of the Options gestures (supersedes the v0.9.58d wiring).\n        var p58exp = V27ReadSrc(Path.Combine(\"Services\", \"PicoFirmwareExporter.cs\"));\n        var p58keys = PicoFirmwareExporter.HotkeyToVirtualKeys(\"Ctrl+Add\");\n        Assert(p58keys.SequenceEqual(new[] { 0xA2, 0x6B }),\n            \"v0.9.58d: Pico exporter converts configured modifier + numpad key\");\n        Assert(p58exp.Contains(\"digitalio.DigitalInOut(board.GP4)\")\n               && p58exp.Contains(\"digitalio.DigitalInOut(board.GP3)\")\n               && p58exp.Contains(\"Keycode.KEYPAD_NUMLOCK\")\n               && p58exp.Contains(\"Keycode.SCROLL_LOCK\"),\n            \"v0.9.60: fixed keypad on GP4/GP3 emits Num Lock and Scroll Lock\");", 1),
    ("tests/TestRunner.cs", "one", "        Console.WriteLine($\"=== Results: {passed} passed, {failed} failed ===\");", "        // ── Step 58: v0.9.60 — hardened firmware consolidation + UTF-8 transport + log multi-select ──\n        Console.WriteLine();\n        Console.WriteLine(\"--- Step 58: v0.9.60 firmware consolidation + transport hardening ---\");\n        var exp60 = V27ReadSrc(Path.Combine(\"Services\", \"PicoFirmwareExporter.cs\"));\n        var brg60 = V27ReadSrc(Path.Combine(\"..\", \"..\", \"bridge\", \"bridge.py\"));\n        var pbb60 = V27ReadSrc(Path.Combine(\"Services\", \"PythonBoardBridge.cs\"));\n        var mwx60 = V27ReadSrc(\"MainWindow.xaml\");\n        var sd60 = V27ReadSrc(Path.Combine(\"Views\", \"StepDialog.xaml.cs\"));\n        var vm60 = V27ReadSrc(Path.Combine(\"ViewModels\", \"MainViewModel.cs\"));\n        var csp60 = V27ReadSrc(\"Ams.UI.csproj\");\n\n        Assert(PicoFirmwareExporter.BundleVersion == \"0.9.60\"\n               && csp60.Contains(\"<Version>0.9.60</Version>\")\n               && vm60.Contains(\"Classroom Studio v0.9.60\")\n               && exp60.Contains(\"BundleVersion = \\\"0.9.60\\\"\"),\n            \"v0.9.60: version pins (firmware bundle, csproj, app banner)\");\n\n        // firmware contract: optional sensor / never-die / byte buffer / arm pump / fixed keypad\n        Assert(exp60.Contains(\"ERR|NOSENSOR\"),\n            \"v0.9.60: WLUX/TRGLUX/LCAL answer ERR|NOSENSOR when the BH1750 is unplugged\");\n        Assert(exp60.Contains(\"AUTOSTART = False\"),\n            \"v0.9.60: the standalone engine never autoruns on boot (AUTOSTART=False)\");\n        Assert(exp60.Contains(\"digitalio.DigitalInOut(board.GP4)\")\n               && exp60.Contains(\"digitalio.DigitalInOut(board.GP3)\")\n               && exp60.Contains(\"Keycode.KEYPAD_NUMLOCK\")\n               && exp60.Contains(\"Keycode.SCROLL_LOCK\"),\n            \"v0.9.60: fixed keypad GP4 = Num Lock start/stop, GP3 = Scroll Lock pause/resume\");\n        Assert(exp60.Contains(\"buffer = bytearray()\") && !exp60.Contains(\"buffer += serial\"),\n            \"v0.9.60: serial RX uses a bounded byte buffer, not string concatenation\");\n        Assert(exp60.Contains(\"def pump_arm()\") && exp60.Contains(\"def forward_fast(\"),\n            \"v0.9.60: permanent arm pump + fire-and-ack mouse path\");\n        Assert(!exp60.Contains(\"KBD_ON_ARM =\")\n               && exp60.Contains(\"line.startswith(\\\"KBDARM|\\\")\")\n               && exp60.Contains(\"line.startswith(\\\"KBDPICO|\\\")\"),\n            \"v0.9.60: keyboard always on the Pico; both legacy envelopes are consumed locally\");\n\n        var code60 = PicoFirmwareExporter.BuildCodePy(new List<PicoFirmwareExporter.LightState>(), \"M\");\n        Assert(code60.Contains(\"AUTOSTART = False\") && code60.Contains(\"pico-light 0.9.60\")\n               && !code60.Contains(\"__KBD_ON_ARM__\") && !code60.Contains(\"__RUNSTOP_HOTKEY__\")\n               && !code60.Contains(\"__PAUSERESUME_HOTKEY__\") && !code60.Contains(\"__LOOP_MODE__\"),\n            \"v0.9.60: exported code.py is fully baked with the fixed control contract\");\n\n        // UTF-8 transport: the bridge must never die while REPORTING an error (the cp1252 crash)\n        Assert(brg60.Contains(\"reconfigure(encoding=\\\"utf-8\\\", errors=\\\"replace\\\")\")\n               && brg60.Contains(\"ensure_ascii=True\"),\n            \"v0.9.60: bridge stdio is forced to UTF-8 with an ASCII-safe emit fallback\");\n        Assert(pbb60.Contains(\"StandardOutputEncoding = Encoding.UTF8\")\n               && pbb60.Contains(\"StandardErrorEncoding = Encoding.UTF8\")\n               && pbb60.Contains(\"PYTHONIOENCODING\"),\n            \"v0.9.60: the C# side reads the bridge as UTF-8 too\");\n\n        // serial log: multi-select so copy-selected-lines is actually usable\n        Assert(mwx60.Contains(\"x:Name=\\\"SerialLogList\\\"\")\n               && System.Text.RegularExpressions.Regex.IsMatch(mwx60,\n                    \"x:Name=\\\"SerialLogList\\\"[^>]*SelectionMode=\\\"Extended\\\"\",\n                    System.Text.RegularExpressions.RegexOptions.Singleline),\n            \"v0.9.60: the serial log allows Ctrl/Shift multi-selection for copy-selected\");\n\n        // fractional stableSec end-to-end\n        var wl60 = new StepNode\n        {\n            Type = \"waitForLight\",\n            Props = new Dictionary<string, object?> { { \"stableSec\", 0.5 } },\n        };\n        var wl60cmds = StepDefinitions.GetCommands(wl60);\n        Assert(wl60cmds.Count == 1 && wl60cmds[0].StartsWith(\"WLUX|\") && wl60cmds[0].Split(',')[2] == \"500\",\n            $\"v0.9.60: stableSec 0.5 becomes 500ms on the wire (got: {string.Join(\", \", wl60cmds)})\");\n        wl60.RefreshSummary();\n        Assert(wl60.Summary.Contains(\"0.5s\"),\n            $\"v0.9.60: the row summary shows the fractional stabilize time (got: {wl60.Summary})\");\n        var wl60int = new StepNode\n        {\n            Type = \"waitForLight\",\n            Props = new Dictionary<string, object?> { { \"stableSec\", 2 } },\n        };\n        Assert(StepDefinitions.GetCommands(wl60int)[0].Split(',')[2] == \"2000\",\n            \"v0.9.60: legacy integer stableSec plans still produce the same wire command\");\n        Assert(sd60.Contains(\"TryParseFloat\") && sd60.Contains(\"CultureInfo.InvariantCulture\"),\n            \"v0.9.60: the step dialog accepts 0.5 regardless of the Windows display language\");\n\n        Console.WriteLine($\"=== Results: {passed} passed, {failed} failed ===\");", 1),
]
PROJECT_STATE = "# Classroom Studio — وضعیت زنده‌ی پروژه\n\n> این سند، منبع حقیقت جاری پروژه است. بعد از هر نسخه به‌روز می‌شود.\n> برگه‌ی Notion («Classroom Studio — پروژه ۲») تاریخچه‌ی کامل جلسات را دارد؛ این فایل وضعیت لحظه‌ای است.\n\n## وضعیت لحظه‌ای — ۲۰۲۶-۰۹-۰۸ (به‌روزرسانی چهارم)\n\n**مبنای فعلی: `main` @ v0.9.60 — CI سبز (`ci-16`: ۶۷۶ پاس / ۰ فیل) + پچ تجمیع v0.9.60**\n\n- ریپو: `pedrampedi81-dotcom/smc` (private) · هر push سبز ← Release خودکار (زیپ + sha256 + لاگ) · هر قرمز ← Issue با خطوط FAIL\n- آخرین Release قبل از این نسخه: **ci-16** (676/0) روی commit `a004df45` (دکمه‌ی کپی لاگ)\n- آخرین نسخه‌ی منسوج: **v0.9.60 — firmware consolidation + transport hardening**\n\n## v0.9.60 چه چیزی را یکجا می‌کند\n\nقرارداد نهایی سخت‌افزار (مصوب ۰۰:۱۷ بامداد ۰۹-۰۸) که قبلاً فقط به‌صورت فایل standalone دست‌آزمایی شده بود (0.9.59h/k/m/n)، حالا وارد سورس و ژنراتور شد:\n\n1. **فرم‌ور پیکو (`PicoFirmwareExporter.CodeTemplate`)** — بازنویسی hardened:\n   - سنسور BH1750 **اختیاری** است؛ بدون سنسور بوت و `PING` کار می‌کند و `WLUX`/`TRGLUX`/`LCAL` ← `ERR|NOSENSOR` (رفع مرگ خاموش بوت با سیم شل‌شده)\n   - بافر سریال **بایتی و کران‌دار** به‌جای الحاق رشته‌ای (رفع خردشدن heap و MemoryError)\n   - **پمپ دائمی بازو** در هر تکرار حلقه و داخل انتظارها: EVTهای پرو میکرو زنده به PC می‌رسند و بافر TX کوچک بازو هرگز پر نمی‌شود (رفع زنجیره‌ی مرگ ۲۲:۲۲)\n   - فرمان‌های موس **fire-and-ack** (فوروارد + پاسخ فوری) تا مسیرهای متراکم انسانی نرم بمانند؛ صدا/SETRES همچنان پاسخ واقعی بازو را منتظر می‌مانند\n   - **کیبورد همیشه روی پیکو**: هر دو envelope ‏`KBDPICO|` و `KBDARM|` محلی مصرف می‌شوند؛ پرو میکرو هرگز تایپ نمی‌کند\n   - **کیپد ثابت** مستقل از Options: ‏GP4→GND = Num Lock = Start/Stop · ‏GP3→GND = Scroll Lock = Pause/Resume (هم موتور standalone و هم HID به PC)\n   - `AUTOSTART = False` پیش‌فرض: کپی/بوت code.py ماکرو را خودکار شروع نمی‌کند؛ موتور standalone تا ۳ ثانیه بعد از آخرین فرمان host ساکت است (ضد اجرای دوبل)\n   - نگهبان **never-die** دور هر خط و کل حلقه (Ctrl+C همچنان کار می‌کند — Exception نیست)\n2. **bridge.py** — خروجی never-die با UTF-8 اجباری (`reconfigure(encoding=\"utf-8\", errors=\"replace\")`) + فالبک `ensure_ascii=True` در emit؛ رفع کرش cp1252 هنگام **گزارش** خطا که خطای واقعی را پنهان می‌کرد\n3. **PythonBoardBridge.cs** — `StandardOutputEncoding`/`StandardErrorEncoding = UTF-8` + `PYTHONIOENCODING=utf-8`؛ لاگ فارسی دیگر mojibake نیست\n4. **UI** — `SelectionMode=\"Extended\"` برای لاگ سریال (کپی خطوط انتخاب‌شده با Ctrl/Shift واقعاً کار می‌کند)\n5. **stableSec اعشاری end-to-end** — `0.5` → ‏`WLUX|...,500,...` · خلاصه‌ی ردیف `0.5s` را نشان می‌دهد (نه int گردشده) · پارس دیالوگ مستقل از locale ویندوز (InvariantCulture + فالبک محلی)\n\nنسخه‌ها: csproj + بنر + `BundleVersion` = **0.9.60** · نگهبان مِتای TestRunner: `curMinor = 60` · گام ۵۸ تست با ۱۵ assertion تازه\n\n## معماری بردها (قرارداد قطعی)\n\n| برد | نقش | پین‌ها |\n| --- | --- | --- |\n| Raspberry Pi Pico | مغز: کیبورد HID + سنسور نور BH1750 + موتور ماکرو standalone | UART0: GP16=TX ← GP17=RX (BSS138) · I2C: SDA=GP20 / SCL=GP21 (0x23) · کیپد: GP4=Start/Stop · GP3=Pause/Resume |\n| Arduino Pro Micro (fw 1.7) | بازو: فقط موس + سنسور صدا | A0 = سنسور صدا · رم ۸۸٪ / فلش ۸۱٪ — فلش ISP با `isp_flash_app.py` انجام شد |\n\n- مسیر عملیاتی همیشه از پیکو می‌گذرد؛ اتصال مستقیم USB به بازو فقط برای سرویس/تشخیص است\n- `PING` روی پیکو: `OK|PONG|pico-light 0.9.60|role=brain+keyboard+light|arm=promicro`\n- پل bridge اول مغز پیکو را پروب می‌کند (متن‌باز، بدون ams_key.json)؛ نبود ← BoardLink رمزشده برای اتصال مستقیم به بازو\n\n## CI/CD\n\n- ورک‌فلو v5.2: `py_compile` روی bridge.py + تست رفتاری `tools/test_bridge_pico.py` + build جداگانه‌ی TestRunner + تست‌ها با خروجی کامل (`2>&1`) · فقط ۰ فیل Release می‌سازد · push فقط-مستنداتی نادیده گرفته می‌شود\n- دارون‌وابسته‌ها روی CI تمیز SKIP می‌شوند (طراحی‌شده)\n\n## قدم بعدی\n\n1. **تست سخت‌افزار v0.9.60:** Export Pico Firmware از اپ buildشده ← کپی روی CIRCUITPY ← `PING` باید `pico-light 0.9.60` بدهد ← تست موس از مسیر پیکو←بازو (باید نرم بماند — پمپ دائمی) ← تست کیپد GP4/GP3 ← تست بوت بدون سنسور\n2. تست صدا: یک‌بار سکوت + یک‌بار صدای بلند Calibrate ← «کپی کل لاگ» ← تفکیک ۱۱–۱۲\n3. بکلاگ: ایمپورتر AMK (دارون۱) · موارد بصری v0.9.46–52 · کاندیدای fw 1.8 بازو: رهاکردن پاسخ Serial1 وقتی TX پر است\n\n## قواعد ثابت (خلاصه)\n\n- هرگز selective merge نه — کل درخت جایگزین می‌شود · بلوک build اجباری + خروجی تازه‌ی TestRunner\n- اول شبیه‌سازی Python، بعد C# · داده قبل از حدس\n- هر نسخه: بنر + csproj + سند docs + ورودی MEMORY.md + نگهبان نسخه در TestRunner\n- آینه‌ی گیت‌هاب برای هر مصنوع (sim/ · uploads/ · tools/)\n"
MEMORY_APPEND = "\n## ۲۰۲۶-۰۹-۰۸ — v0.9.60 (تجمیع فرم‌ور + هاردنینگ انتقال)\n\n- `code.py` (از طریق `PicoFirmwareExporter`): سنسور BH1750 اختیاری (`ERR|NOSENSOR`) · بافر بایتی کران‌دار · پمپ دائمی بازو در هر تکرار حلقه و داخل انتظارها (ضد back-pressure؛ زنجیره‌ی مرگ ۲۲:۲۲ مورخ ۰۹-۰۷) · موس fire-and-ack · کیبورد همیشه روی Pico و هر دو envelope قدیمی محلی مصرف می‌شوند · کیپد ثابت GP4=Num Lock (Start/Stop) و GP3=Scroll Lock (Pause/Resume) · `AUTOSTART=False` + سکوت ۳ ثانیه‌ای بعد از آخرین فرمان host (ضد اجرای دوبل) · نگهبان never-die.\n- `bridge.py`: خروجی UTF-8 never-die (`reconfigure` + فالبک `ensure_ascii=True`) — رفع کرش cp1252 هنگام گزارش خطای واقعی.\n- `PythonBoardBridge.cs`: خواندن UTF-8 + `PYTHONIOENCODING` — لاگ فارسی بدون mojibake.\n- UI: `SelectionMode=\"Extended\"` برای لاگ سریال · `stableSec` اعشاری کامل (فرمان 500ms + خلاصه‌ی «0.5s» + پارس مستقل از locale).\n- تصمیم و ردشده: قالب Options-driven کیپد (v0.9.58d) نگه داشته نشد چون قرارداد سخت‌افزاری نهایی (۰۰:۱۷ بامداد ۰۹-۰۸) کنترل‌های ثابت GP4/GP3 را می‌خواهد؛ `HotkeyToVirtualKeys` به‌عنوان ابزار باقی ماند ولی دیگر در فرم‌ور bake نمی‌شود.\n- اعتبارسنجی این‌طرف: شبیه‌سازی سخت‌افزار ساختگی ۲۳/۰ سبز (sim60) + py_compile قالب پرشده + پچ idempotent.\n"
DOC_NOTE = "# v0.9.60 — firmware consolidation + transport hardening\n\nاین نسخه قرارداد نهایی سخت‌افزار (نسخه‌های دست‌آزمایی‌شده‌ی 0.9.59h/k/m/n) را وارد سورس و\nژنراتور `PicoFirmwareExporter` می‌کند و دو نقص انتقالی را می‌بندد.\n\n## فرم‌ور پیکو (code.py تولیدی)\n\n- **سنسور اختیاری:** بوت و `PING` بدون BH1750 کار می‌کنند؛ `WLUX`/`TRGLUX`/`LCAL` ← `ERR|NOSENSOR|...`\n- **بافر بایتی کران‌دار** به‌جای الحاق رشته‌ای؛ نگهبان never-die دور هر خط و کل حلقه\n- **پمپ دائمی بازو:** در هر تکرار حلقه و داخل `wait_range`/`LCAL`/`forward_to_arm` —\n  EVTهای بازو زنده به PC می‌رسند و بافر TX بازو هرگز پر و قفل نمی‌شود\n- **موس fire-and-ack:** ‏`MMOVE`/`MCLICK`/`MWHEEL`/`MDOWN`/`MUP` فوروارد + پاسخ فوری؛\n  صدا و SETRES و HALT/BYE پاسخ واقعی بازو را منتظر می‌مانند\n- **کیبورد همیشه Pico:** ‏`KBDPICO|` و `KBDARM|` هر دو محلی مصرف می‌شوند\n- **کیپد ثابت:** GP4→GND = Num Lock = Start/Stop · GP3→GND = Scroll Lock = Pause/Resume\n  (هم موتور standalone و هم کلید HID به PC)\n- **امن پیش‌فرض:** `AUTOSTART = False`؛ موتور standalone تا ۳ ثانیه بعد از آخرین فرمان host ساکت است\n\n## انتقال\n\n- `bridge.py`: stdio اجباراً UTF-8 با errors=replace + فالبک ASCII در emit (گزارش‌گر هرگز نمی‌میرد)\n- `PythonBoardBridge.cs`: ‏`StandardOutputEncoding`/`StandardErrorEncoding = Encoding.UTF8` + `PYTHONIOENCODING=utf-8`\n\n## UI و داده\n\n- لاگ سریال: `SelectionMode=\"Extended\"` (کپی خطوط انتخاب‌شده واقعی شد)\n- `stableSec` اعشاری: ‏0.5 → 500ms روی سیم، خلاصه‌ی «0.5s»، پارس InvariantCulture با فالبک محلی\n\n## تست‌ها\n\n- گام ۵۸ TestRunner: ۱۵ assertion (پین‌های نسخه، قرارداد فرم‌ور، UTF-8 هر دو طرف، multi-select، stableSec)\n- گام ۴۴ به قرارداد جدید کیبورد به‌روز شد (بدون `KBD_ON_ARM`) · گام ۵۷(b) به کیپد ثابت GP4/GP3\n- شبیه‌سازی سخت‌افزار ساختگی این‌طرف: ۲۳/۰ سبز\n"
SIM_SOURCE = "#!/usr/bin/env python3\n\"\"\"sim60.py — shبیه‌سازی سخت‌افزار ساختگی برای قالب فرم‌ور v0.9.60 (قاعده‌ی ۳ طلایی).\n\nماژول‌های CircuitPython (board/busio/usb_cdc/usb_hid/digitalio/adafruit_hid) با\nاستاب‌های ضبط‌کننده جایگزین می‌شوند، قالب با placeholderهای پرشده exec می‌شود و\nحلقه‌ی اصلی با یک BaseException (SimExit) بعد از N خواب ساختگی متوقف می‌شود —\nهمان‌طور که Ctrl+C واقعی هم از تور never-die عبور می‌کند (Exception نیست).\n\"\"\"\nimport io\nimport json\nimport sys\nimport types\n\nTEMPLATE = open(\"/data/firmware_template.py\", encoding=\"utf-8\").read()\nFILLED = (TEMPLATE\n          .replace(\"__MACHINE__\", \"SIM\")\n          .replace(\"__GENERATED__\", \"2026-09-08 01:30\")\n          .replace(\"__VERSION__\", \"0.9.60\")\n          .replace(\"__STATE_COUNT__\", \"1\")\n          .replace(\"__LOOP_MODE__\", \"forever\")\n          .replace(\"__LOOP_COUNT__\", \"0\")\n          .replace(\"__LOOP_SECONDS__\", \"0\"))\n\n# 0) syntax gate first (py_compile معادل — بدون import)\ncompile(FILLED, \"code.py\", \"exec\")\nprint(\"PASS: template compiles (filled)\")\n\nresults = []\n\n\ndef check(cond, msg):\n    results.append((bool(cond), msg))\n    print((\"PASS: \" if cond else \"FAIL: \") + msg)\n\n\nclass SimExit(BaseException):\n    pass\n\n\ndef run_scenario(name, sensor_ok=True, lux=1200.0, arm_ok=True, feed=b\"\",\n                 calib=None, sleep_limit=3000, btn_script=None, arm_evt=False):\n    clock = [100.0]\n    sleeps = [0]\n\n    t = types.ModuleType(\"time\")\n    t.monotonic = lambda: clock[0]\n\n    def _sleep(s):\n        clock[0] += s\n        sleeps[0] += 1\n        if btn_script:\n            btn_script(sleeps[0], btns)\n        if sleeps[0] > sleep_limit:\n            raise SimExit()\n    t.sleep = _sleep\n\n    board = types.ModuleType(\"board\")\n    for p in (\"GP21\", \"GP20\", \"GP16\", \"GP17\", \"GP4\", \"GP3\"):\n        setattr(board, p, p)\n\n    class FakeI2C:\n        def __init__(self, *a):\n            if not sensor_ok:\n                raise OSError(\"no sensor on the bus\")\n        def try_lock(self):\n            return True\n        def unlock(self):\n            pass\n        def writeto(self, addr, b):\n            pass\n        def readfrom_into(self, addr, buf):\n            raw = int(lux * 1.2)\n            buf[0] = (raw >> 8) & 0xFF\n            buf[1] = raw & 0xFF\n\n    class FakeUART:\n        def __init__(self, *a, **k):\n            if not arm_ok:\n                raise OSError(\"no arm\")\n            self.written = []\n            self._sched = []          # (ready_at, bytes)\n\n        @property\n        def in_waiting(self):\n            return sum(len(b) for at, b in self._sched if at <= clock[0])\n\n        def read(self, n=None):\n            out = b\"\"\n            keep = []\n            for at, b in self._sched:\n                if at <= clock[0]:\n                    out += b\n                else:\n                    keep.append((at, b))\n            self._sched = keep\n            return out\n\n        def write(self, b):\n            line = b.decode().strip()\n            self.written.append(line)\n            head = line.split(\"|\")[0]\n            if head == \"WSND\":\n                if arm_evt:\n                    self._sched.append((clock[0] + 0.02, b\"EVT|TRG|react=90\\n\"))\n                self._sched.append((clock[0] + 0.05, b\"OK|WSND\\n\"))\n            elif head in (\"MMOVE\", \"MCLICK\", \"MWHEEL\", \"MDOWN\", \"MUP\"):\n                self._sched.append((clock[0] + 0.10, (\"OK|\" + head + \"\\n\").encode()))\n            elif line in (\"HALT\", \"BYE\"):\n                self._sched.append((clock[0] + 0.01, (\"OK|\" + line + \"\\n\").encode()))\n            return len(b)\n\n        def inject(self, line, delay=0.0):\n            self._sched.append((clock[0] + delay, line.encode() + b\"\\n\"))\n\n    busio = types.ModuleType(\"busio\")\n    busio.I2C = FakeI2C\n    busio.UART = FakeUART\n\n    class FakeSerial:\n        def __init__(self):\n            self._in = bytearray(feed)\n            self.lines = []\n\n        @property\n        def in_waiting(self):\n            return min(3, len(self._in))      # drip-feed: 3 bytes per poll\n\n        def read(self, n):\n            n = min(n or 0, len(self._in))\n            out = bytes(self._in[:n])\n            del self._in[:n]\n            return out\n\n        def write(self, b):\n            self.lines.append(b.decode().strip())\n            return len(b)\n\n    serial_obj = FakeSerial()\n    usb_cdc = types.ModuleType(\"usb_cdc\")\n    usb_cdc.data = serial_obj\n    usb_cdc.console = None\n    usb_hid = types.ModuleType(\"usb_hid\")\n    usb_hid.devices = []\n\n    kbd_log = {\"press\": [], \"release\": [], \"write\": []}\n\n    class FakeKeyboard:\n        def __init__(self, devices):\n            pass\n        def press(self, code):\n            kbd_log[\"press\"].append(code)\n        def release(self, code):\n            kbd_log[\"release\"].append(code)\n        def write(self, ch):\n            kbd_log[\"write\"].append(ch)\n\n    kc = types.SimpleNamespace()\n    for i, ch in enumerate(\"ABCDEFGHIJKLMNOPQRSTUVWXYZ\"):\n        setattr(kc, ch, 1000 + i)\n    for i, nm in enumerate((\"ZERO\", \"ONE\", \"TWO\", \"THREE\", \"FOUR\", \"FIVE\", \"SIX\", \"SEVEN\", \"EIGHT\", \"NINE\")):\n        setattr(kc, nm, 1100 + i)\n    for i in range(1, 25):\n        setattr(kc, \"F\" + str(i), 1200 + i)\n    kc.ENTER = 13; kc.ESCAPE = 27; kc.SPACE = 32; kc.TAB = 9; kc.BACKSPACE = 8\n    kc.LEFT_ARROW = 37; kc.RIGHT_ARROW = 39; kc.UP_ARROW = 38; kc.DOWN_ARROW = 40\n    kc.LEFT_SHIFT = 160; kc.RIGHT_SHIFT = 161; kc.LEFT_CONTROL = 162; kc.RIGHT_CONTROL = 163\n    kc.LEFT_ALT = 164; kc.RIGHT_ALT = 165; kc.LEFT_GUI = 91; kc.RIGHT_GUI = 92\n    kc.E = 1000 + ord(\"E\") - ord(\"A\")\n    kc.KEYPAD_NUMLOCK = 2001\n    kc.SCROLL_LOCK = 2002\n\n    ah = types.ModuleType(\"adafruit_hid\")\n    ah_kbd = types.ModuleType(\"adafruit_hid.keyboard\")\n    ah_kbd.Keyboard = FakeKeyboard\n    ah_kc = types.ModuleType(\"adafruit_hid.keycode\")\n    ah_kc.Keycode = kc\n\n    class FakeBtn:\n        def __init__(self, pin):\n            self.pin = pin\n            self.value = True               # pull-up: True = released\n            self.direction = None\n            self.pull = None\n\n    btns = {\"GP4\": FakeBtn(\"GP4\"), \"GP3\": FakeBtn(\"GP3\")}\n    dio = types.ModuleType(\"digitalio\")\n    dio.DigitalInOut = lambda pin: btns[pin]\n    dio.Direction = types.SimpleNamespace(INPUT=\"in\")\n    dio.Pull = types.SimpleNamespace(UP=\"up\")\n\n    def fake_open(path, mode=\"r\"):\n        if path == \"/pico-calibration.json\":\n            if calib is None:\n                raise OSError(\"no calibration file\")\n            return io.StringIO(json.dumps(calib))\n        raise OSError(\"unexpected open: \" + path)\n\n    saved = {}\n    mods = {\"time\": t, \"board\": board, \"busio\": busio, \"usb_cdc\": usb_cdc,\n            \"usb_hid\": usb_hid, \"digitalio\": dio,\n            \"adafruit_hid\": ah, \"adafruit_hid.keyboard\": ah_kbd,\n            \"adafruit_hid.keycode\": ah_kc}\n    for m, v in mods.items():\n        saved[m] = sys.modules.get(m)\n        sys.modules[m] = v\n    g = {\"__name__\": \"__main__\", \"open\": fake_open}\n    arm_obj = None\n    try:\n        try:\n            exec(compile(FILLED, \"code.py\", \"exec\"), g)\n        except SimExit:\n            pass\n    finally:\n        for m in mods:\n            if saved[m] is None:\n                sys.modules.pop(m, None)\n            else:\n                sys.modules[m] = saved[m]\n    print(f\"--- scenario {name}: {sleeps[0]} fake sleeps, {len(serial_obj.lines)} PC lines\")\n    return g, serial_obj, kbd_log, btns\n\n\n# ── A: full hardware, mixed command stream (split-feed included by the drip) ──\ng, ser, kbd, btns = run_scenario(\n    \"A full stack\",\n    feed=(b\"PING\\n\"\n          b\"WLUX|100,2000,100,5000,0\\n\"\n          b\"MCLICK|left,1\\n\"\n          b\"WSND|90,60,2000\\n\"\n          b\"KBDARM|KCOMBO|162+115,40,40\\n\"\n          b\"KDOWN|x\\n\"                       # malformed on purpose -> ERR|EXC, loop must survive\n          b\"LCAL|100\\n\"\n          b\"HALT\\n\"))\nlines = ser.lines\ncheck(any(l.startswith(\"OK|PONG|pico-light 0.9.60\") and \"role=brain\" in l for l in lines),\n      \"A1: PING answered with the 0.9.60 brain identity\")\ncheck(\"OK|WLUX|lux=1200\" in lines, \"A2: WLUX stabilized at the fake 1200 lux\")\ncheck(lines.count(\"OK|MCLICK\") == 1, \"A3: MCLICK fire-and-ack answered exactly once (arm's own OK discarded by the pump)\")\ncheck(g[\"arm\"].written.count(\"MCLICK|left,1\") == 1, \"A4: MCLICK was forwarded to the arm\")\ncheck(\"OK|WSND\" in lines, \"A5: blocking WSND returned the arm's real reply\")\ncheck(g[\"arm\"].written.count(\"WSND|90,60,2000\") == 1, \"A6: WSND forwarded to the arm\")\ncheck(\"OK|KCOMBO\" in lines and 162 in kbd[\"press\"] and 1204 in kbd[\"press\"],\n      \"A7: legacy KBDARM envelope consumed LOCALLY (Ctrl+F4 typed on the Pico)\")\ncheck(not any(w.startswith(\"KCOMBO\") for w in g[\"arm\"].written),\n      \"A8: no keyboard command ever reaches the arm (final contract)\")\ncheck(\"ERR|EXC|KDOWN\" in lines, \"A9: a malformed line yields ERR|EXC and the loop survives\")\ncheck(any(l.startswith(\"OK|LCAL|min=1200|max=1200|avg=1200\") for l in lines),\n      \"A10: LCAL reports min/max/avg\")\ncheck(\"OK|HALT\" in lines and \"HALT\" in g[\"arm\"].written, \"A11: HALT acked and forwarded to the arm\")\n\n# arm EVT streams live to the PC, even while a blocking command waits for its reply\ng3, ser3, _, _ = run_scenario(\"A12 arm event\", feed=b\"PING\\nWSND|90,60,2000\\n\", arm_evt=True)\nlines3 = ser3.lines\ncheck(\"EVT|TRG|react=90\" in lines3 and \"OK|WSND\" in lines3\n      and lines3.index(\"EVT|TRG|react=90\") < lines3.index(\"OK|WSND\"),\n      \"A12: an arm EVT streams to the PC live, before the blocking command's reply\")\n\n# ── B: sensor unplugged — the brain must stay alive ──\ngB, serB, kbdB, _ = run_scenario(\"B no sensor\", sensor_ok=False,\n                                 feed=b\"PING\\nWLUX|1,2,3,4,0\\nLCAL|100\\nTRGLUX|1,2,3,4,0,69,0,0,40,40\\n\")\ncheck(any(l.startswith(\"OK|PONG|\") for l in serB.lines), \"B1: PING answered without the sensor\")\ncheck(\"ERR|NOSENSOR|WLUX\" in serB.lines, \"B2: WLUX -> ERR|NOSENSOR\")\ncheck(\"ERR|NOSENSOR|LCAL\" in serB.lines, \"B3: LCAL -> ERR|NOSENSOR\")\ncheck(\"ERR|NOSENSOR|TRGLUX\" in serB.lines, \"B4: TRGLUX -> ERR|NOSENSOR\")\ncheck(len(kbdB[\"press\"]) == 0, \"B5: no phantom keypresses without a sensor\")\n\n# ── C: fixed keypad GP4/GP3 ──\ndef btn_script_c(n, b):\n    if n == 50: b[\"GP4\"].value = False      # press Start/Stop\n    if n == 60: b[\"GP4\"].value = True\n    if n == 70: b[\"GP3\"].value = False      # press Pause/Resume\n    if n == 80: b[\"GP3\"].value = True\n\ngC, serC, kbdC, _ = run_scenario(\"C keypad\", feed=b\"\", btn_script=btn_script_c)\ncheck(kbdC[\"press\"].count(2001) == 1, \"C1: GP4 tap sends exactly one Num Lock to the PC\")\ncheck(kbdC[\"press\"].count(2002) == 1, \"C2: GP3 tap sends exactly one Scroll Lock to the PC\")\ncheck(gC[\"engine_on\"] is True and gC[\"engine_paused\"] is True,\n      \"C3: GP4 started the standalone engine, GP3 paused it\")\n\n# ── D: standalone engine fires armed states only when enabled ──\ncalib = {\"states\": [{\"name\": \"s1\", \"luxLow\": 100, \"luxHigh\": 2000, \"stableMs\": 100,\n                     \"timeoutMs\": 20000, \"mode\": 0, \"keyVk\": 69, \"key\": \"E\", \"armed\": True}]}\ngD, serD, kbdD, _ = run_scenario(\"D standalone idle\", calib=calib, feed=b\"\", sleep_limit=200)\ncheck(len(kbdD[\"press\"]) == 0, \"D1: AUTOSTART=False — boot never starts the macro by itself\")\n\ndef btn_script_d2(n, b):\n    # only GP4 (Start) — no pause press; the engine must fire after the 3s post-boot quiet window\n    if n == 50: b[\"GP4\"].value = False\n    if n == 60: b[\"GP4\"].value = True\n\ngE, serE, kbdE, _ = run_scenario(\"D2 standalone armed\", calib=calib, feed=b\"\",\n                                 sleep_limit=400, btn_script=btn_script_d2)\ncheck(kbdE[\"press\"].count(1000 + ord(\"E\") - ord(\"A\")) >= 1,\n      \"D2: after GP4 the engine presses the armed state key (E)\")\ncheck(gE[\"passes\"] >= 1, \"D3: standalone pass counter advanced\")\n\nfailed = [m for ok, m in results if not ok]\nprint()\nprint(f\"=== sim60: {len(results) - len(failed)} passed, {len(failed)} failed ===\")\nsys.exit(1 if failed else 0)\n"
FIRMWARE_TEMPLATE = "# Classroom Studio v__VERSION__ - Raspberry Pi Pico light-sensor firmware (CircuitPython)\n# System: __MACHINE__   generated: __GENERATED__   calibrated states: __STATE_COUNT__\n# Sensor: BH1750 (GY-302 / GY-30) on I2C0 - SDA=GP20, SCL=GP21, ADDR->GND => 0x23\n# Board roles: the Pico is the executive brain - keyboard (USB HID) + light sensor + macro\n# engine. Mouse and the sound sensor belong to the Arduino Pro Micro, which the Pico drives\n# as its arm over UART0: GP16 = TX -> Pro Micro RX, GP17 = RX <- Pro Micro TX, common GND,\n# 115200 8N1.\n#\n# v0.9.60 - hardened consolidation (bench-proven as the 0.9.59h/k/m/n line):\n#   * BH1750 OPTIONAL: the brain boots and answers PING even with the sensor unplugged;\n#     WLUX/TRGLUX/LCAL then answer ERR|NOSENSOR and armed light states stay silent.\n#   * Serial RX is a bounded BYTE buffer - no per-read string concat, no heap churn.\n#   * The Pro Micro arm is pumped EVERY loop iteration: its EVT| lines stream to the PC\n#     live and its small TX buffer can never fill up and wedge the arm.\n#   * Mouse commands are fire-and-ack (forwarded to the arm, OK answered at once) so dense\n#     human paths stay smooth; sound/SETRES still wait for the arm's real reply.\n#   * The keyboard ALWAYS runs on this Pico: KBDPICO| and the legacy arm keyboard envelope\n#     are both consumed locally - the arm never types.\n#   * Fixed keypad: GP4->GND = Num Lock = Start/Stop, GP3->GND = Scroll Lock = Pause/Resume.\n#     Both drive the standalone engine AND send the HID key to the PC.\n#   * Safe default: boot never starts the macro. The standalone engine waits for GP4\n#     (Num Lock) unless AUTOSTART below is explicitly True, and stays silent for 3 s after\n#     the last host command so host-driven runs never double-fire the armed states.\n#\n# Copy code.py + boot.py + pico-calibration.json onto CIRCUITPY and put the\n# adafruit_hid package into /lib. Full instructions: README-FLASH.md\n\nimport time\nimport random\nimport json\nimport board\nimport busio\nimport usb_cdc\nimport usb_hid\nfrom adafruit_hid.keyboard import Keyboard\nfrom adafruit_hid.keycode import Keycode\n\nADDR = 0x23\nPOWER_ON = 0x01\nRESET = 0x07\nCONT_HIRES = 0x10   # 1 lux, ~120 ms per sample (max 180 ms)\nCONT_LORES = 0x13   # 4 lux, ~16 ms per sample\n\n# Play Options baked at export time: how often the armed standalone states run.\nLOOP_MODE = \"__LOOP_MODE__\"      # once | times | timed | forever\nLOOP_COUNT = __LOOP_COUNT__      # passes for \"times\"\nLOOP_SECONDS = __LOOP_SECONDS__  # seconds for \"timed\"\n\n# v0.9.60 - safe default: copying/booting code.py NEVER starts the macro by itself.\n# Set True only for a deliberate power-on-autorun scenario.\nAUTOSTART = False\n\nSPECIAL_VK = {\n    0x0D: Keycode.ENTER, 0x1B: Keycode.ESCAPE, 0x20: Keycode.SPACE, 0x09: Keycode.TAB,\n    0x08: Keycode.BACKSPACE, 0x25: Keycode.LEFT_ARROW, 0x27: Keycode.RIGHT_ARROW,\n    0x26: Keycode.UP_ARROW, 0x28: Keycode.DOWN_ARROW,\n}\n# modifiers too, so KCOMBO shortcuts (Ctrl/Shift/Alt/Win + key) work on the Pico\nMOD_VK = {\n    0xA0: Keycode.LEFT_SHIFT, 0xA1: Keycode.RIGHT_SHIFT,\n    0xA2: Keycode.LEFT_CONTROL, 0xA3: Keycode.RIGHT_CONTROL,\n    0xA4: Keycode.LEFT_ALT, 0xA5: Keycode.RIGHT_ALT,\n    0x5B: Keycode.LEFT_GUI, 0x5C: Keycode.RIGHT_GUI,\n}\nDIGITS = (\"ZERO\", \"ONE\", \"TWO\", \"THREE\", \"FOUR\", \"FIVE\", \"SIX\", \"SEVEN\", \"EIGHT\", \"NINE\")\n\n\ndef keycode_for_vk(vk):\n    if 0x41 <= vk <= 0x5A:\n        return getattr(Keycode, chr(vk))\n    if 0x30 <= vk <= 0x39:\n        return getattr(Keycode, DIGITS[vk - 0x30])\n    if 0x70 <= vk <= 0x7B:\n        return getattr(Keycode, \"F\" + str(vk - 0x6F))\n    return SPECIAL_VK.get(vk, MOD_VK.get(vk, Keycode.E))   # modifiers included\n\n\nclass Bh1750:\n    def __init__(self, i2c, mode=CONT_HIRES):\n        self._i2c = i2c\n        self._buf = bytearray(2)\n        self.mode = None\n        self._cmd(POWER_ON)\n        self._cmd(RESET)\n        self.set_mode(mode)\n\n    def _cmd(self, value):\n        while not self._i2c.try_lock():\n            pass\n        try:\n            self._i2c.writeto(ADDR, bytes([value]))\n        finally:\n            self._i2c.unlock()\n\n    def set_mode(self, mode):\n        if mode == self.mode:\n            return\n        self.mode = mode\n        self._cmd(mode)          # sent once per mode change, never per sample\n        time.sleep(0.2 if mode == CONT_HIRES else 0.05)\n\n    def lux(self):\n        while not self._i2c.try_lock():\n            pass\n        try:\n            self._i2c.readfrom_into(ADDR, self._buf)\n        finally:\n            self._i2c.unlock()\n        raw = (self._buf[0] << 8) | self._buf[1]\n        return raw / 1.2         # datasheet conversion\n\n\ndef median(values):\n    ordered = sorted(values)\n    return ordered[len(ordered) // 2]\n\n\ndef load_states():\n    try:\n        with open(\"/pico-calibration.json\", \"r\") as fh:\n            return json.load(fh).get(\"states\", [])\n    except Exception:\n        return []\n\n\nkbd = Keyboard(usb_hid.devices)\nserial = usb_cdc.data if usb_cdc.data is not None else usb_cdc.console\nstates = load_states()\nwindow = []\n\n# v0.9.60 - the light sensor is OPTIONAL. A loose SDA/SCL wire must never kill the brain\n# before its command loop (that was the silent boot death): without the sensor the light\n# commands answer ERR|NOSENSOR and everything else keeps working.\ntry:\n    i2c = busio.I2C(board.GP21, board.GP20)   # SCL, SDA\n    sensor = Bh1750(i2c)\nexcept Exception:\n    sensor = None\n\n# Commands that are not the brain's own job: forwarded to the Pro Micro arm.\nARM_PREFIXES = (\"MMOVE\", \"MCLICK\", \"MWHEEL\", \"MDOWN\", \"MUP\", \"SETRES\", \"WSND\", \"TRGSND\", \"SCAL\")\n# v0.9.60 - mouse goes fire-and-ack (smooth dense paths); the rest waits for the arm reply.\nMOUSE_PREFIXES = (\"MMOVE\", \"MCLICK\", \"MWHEEL\", \"MDOWN\", \"MUP\")\n# Keyboard commands: ALWAYS typed locally by this Pico (final contract).\nKBD_PREFIXES = (\"KTEXT\", \"KCOMBO\", \"KDOWN\", \"KUP\")\n\ntry:\n    arm = busio.UART(board.GP16, board.GP17, baudrate=115200, timeout=0.2)\nexcept Exception:\n    arm = None\n\n# v0.9.60 - fixed keypad (contract): GP4->GND = Num Lock = Start/Stop,\n# GP3->GND = Scroll Lock = Pause/Resume. Active-low with pull-ups, edge-detected.\ntry:\n    import digitalio\n    btn1 = digitalio.DigitalInOut(board.GP4)\n    btn1.direction = digitalio.Direction.INPUT\n    btn1.pull = digitalio.Pull.UP\n    btn2 = digitalio.DigitalInOut(board.GP3)\n    btn2.direction = digitalio.Direction.INPUT\n    btn2.pull = digitalio.Pull.UP\n    _last_btn1 = True\n    _last_btn2 = True\nexcept Exception:\n    btn1 = btn2 = None\n    _last_btn1 = _last_btn2 = True\n\nengine_on = AUTOSTART        # GP4 toggles this (Num Lock = Start/Stop)\nengine_paused = False        # GP3 toggles this (Scroll Lock = Pause/Resume)\nlast_host_cmd = time.monotonic()\n\n\ndef _serial_write_line(text):\n    try:\n        serial.write((text + \"\\n\").encode(\"utf-8\"))\n    except Exception:\n        pass\n\n\n_arm_buf = bytearray()\n\n\ndef pump_arm():\n    \"\"\"v0.9.60 - permanent arm pump. Called on EVERY loop iteration (and inside every\n    blocking wait) so the Pro Micro's small TX buffer can never fill up and wedge it.\n    Arm EVT| lines stream to the PC live; fire-acked mouse OKs are discarded; every\n    other reply is returned for a waiting forward_to_arm.\"\"\"\n    if arm is None:\n        return []\n    try:\n        n = arm.in_waiting\n        if n:\n            _arm_buf.extend(arm.read(n))\n            if len(_arm_buf) > 1024:              # runaway-garbage guard\n                del _arm_buf[:-256]\n    except Exception:\n        return []\n    ready = []\n    while True:\n        nl = _arm_buf.find(b\"\\n\")\n        if nl < 0:\n            break\n        raw = bytes(_arm_buf[:nl])\n        del _arm_buf[:nl + 1]\n        line = raw.decode(\"utf-8\", \"replace\").strip()\n        if not line:\n            continue\n        if line.startswith(\"EVT|\"):\n            _serial_write_line(line)              # arm events reach the PC live\n            continue\n        parts = line.split(\"|\")\n        if len(parts) > 1 and parts[0] == \"OK\" and parts[1] in MOUSE_PREFIXES:\n            continue                              # the PC already got its fire-and-ack\n        ready.append(line)\n    return ready\n\n\ndef _arm_write(line):\n    if arm is None:\n        return False\n    try:\n        arm.write((line + \"\\n\").encode(\"utf-8\"))\n        return True\n    except Exception:\n        return False\n\n\ndef forward_fast(line):\n    \"\"\"v0.9.60 - mouse fast path: forward to the arm and answer the PC at once; the\n    pump later collects and discards the arm's own OK.\"\"\"\n    head = line.split(\"|\")[0]\n    if not _arm_write(line):\n        return \"ERR|NOARM|\" + head\n    return \"OK|\" + head\n\n\ndef forward_to_arm(line, timeout_s):\n    \"\"\"Blocking forward for commands whose reply the PC needs (sound, SETRES, HALT/BYE).\n    Keeps pumping while waiting so arm EVT| lines still stream to the PC.\"\"\"\n    head = line.split(\"|\")[0]\n    if not _arm_write(line):\n        return \"ERR|NOARM|\" + head\n    end = time.monotonic() + timeout_s\n    while time.monotonic() < end:\n        for reply in pump_arm():\n            # commands are strictly serialized and mouse OKs are already filtered:\n            # the first non-mouse OK/ERR we see belongs to this command.\n            if reply.split(\"|\")[0] in (\"OK\", \"ERR\"):\n                return reply\n        time.sleep(0.005)\n    return \"ERR|TIMEOUT|\" + head\n\n\ndef sample():\n    window.append(sensor.lux())\n    if len(window) > 5:\n        window.pop(0)\n    return median(window)\n\n\ndef press(vk, hold_ms):\n    code = keycode_for_vk(vk)\n    kbd.press(code)\n    time.sleep(max(10, hold_ms) / 1000)\n    kbd.release(code)\n\n\ndef tap_key(code):\n    \"\"\"v0.9.60 - one fixed keypad tap (Num Lock / Scroll Lock) towards the PC.\"\"\"\n    try:\n        kbd.press(code)\n        time.sleep(0.04)\n        kbd.release(code)\n    except Exception:\n        pass\n\n\ndef poll_keypad():\n    \"\"\"v0.9.60 - fixed mapping, independent of the app's Options: GP4 toggles the\n    standalone engine (and sends Num Lock), GP3 toggles pause (and sends Scroll Lock).\"\"\"\n    global engine_on, engine_paused, _last_btn1, _last_btn2\n    if btn1 is None:\n        return\n    b1 = not btn1.value\n    b2 = not btn2.value\n    if b1 and not _last_btn1:\n        engine_on = not engine_on\n        if not engine_on:\n            engine_paused = False\n        tap_key(Keycode.KEYPAD_NUMLOCK)\n    if b2 and not _last_btn2:\n        engine_paused = not engine_paused\n        tap_key(Keycode.SCROLL_LOCK)\n    _last_btn1 = b1\n    _last_btn2 = b2\n\n\ndef wait_range(lo, hi, stable_ms, timeout_ms, mode):\n    sensor.set_mode(CONT_LORES if mode == 1 else CONT_HIRES)\n    deadline = time.monotonic() + timeout_ms / 1000\n    inside_since = None\n    while time.monotonic() < deadline:\n        pump_arm()                            # v0.9.60 - the arm is drained even mid-wait\n        value = sample()\n        if lo <= value <= hi:\n            if inside_since is None:\n                inside_since = time.monotonic()\n            elif (time.monotonic() - inside_since) * 1000 >= stable_ms:\n                return value      # stabilized inside the range for n seconds\n        else:\n            inside_since = None\n        time.sleep(0.02)\n    return None\n\n\ndef ints(fields, count, default=0):\n    out = []\n    for i in range(count):\n        try:\n            out.append(int(fields[i]))\n        except Exception:\n            out.append(default)\n    return out\n\n\ndef handle_keyboard(line, head):\n    # The Pico owns the keyboard (final contract) - always local execution.\n    if head == \"KDOWN\":\n        kbd.press(keycode_for_vk(int(line.split(\"|\")[1])))\n        return \"OK|KDOWN\"\n    if head == \"KUP\":\n        kbd.release(keycode_for_vk(int(line.split(\"|\")[1])))\n        return \"OK|KUP\"\n    if head == \"KCOMBO\":\n        # KCOMBO|vk+vk+...[,hmin,hmax] - press in order, random hold, release in reverse\n        body = line.split(\"|\", 1)[1]\n        hold = 0.0\n        if \",\" in body:\n            body, holdpart = body.split(\",\", 1)\n            hp = holdpart.split(\",\")\n            try:\n                h0 = int(hp[0])\n                h1 = int(hp[1]) if len(hp) > 1 else h0\n                hold = random.uniform(max(0, h0), max(0, h1)) if max(h0, h1) > 0 else 0.0\n            except Exception:\n                hold = 0.0\n        codes = [keycode_for_vk(int(v)) for v in body.split(\"+\") if v]\n        for c in codes:\n            kbd.press(c)\n        time.sleep(max(10.0, hold) / 1000)\n        for c in reversed(codes):\n            kbd.release(c)\n        return \"OK|KCOMBO\"\n    if head == \"KTEXT\":\n        # KTEXT|hmin,hmax,text - ASCII only, per-key random delay\n        parts = line.split(\"|\", 1)[1].split(\",\", 2)\n        try:\n            hmin, hmax = int(parts[0]), int(parts[1])\n        except Exception:\n            hmin, hmax = 0, 0\n        txt = parts[2] if len(parts) > 2 else \"\"\n        for ch in txt:\n            if ord(ch) < 32 or ord(ch) > 126:\n                return \"ERR|ASCII|KTEXT\"\n        for ch in txt:\n            kbd.write(ch)\n            if hmax > 0:\n                time.sleep(random.uniform(max(0, hmin), hmax) / 1000)\n        return \"OK|KTEXT\"\n    return \"ERR|UNKNOWN|\" + head\n\n\ndef handle(line):\n    if line == \"PING\":\n        return \"OK|PONG|pico-light __VERSION__|role=brain+keyboard+light|arm=promicro\"\n    if line.startswith(\"LCAL|\"):\n        if sensor is None:\n            return \"ERR|NOSENSOR|LCAL\"\n        ms = ints(line.split(\"|\")[1].split(\",\"), 1, 2000)[0] or 2000\n        sensor.set_mode(CONT_HIRES)\n        end = time.monotonic() + ms / 1000\n        readings = []\n        while time.monotonic() < end:\n            pump_arm()                        # v0.9.60 - the arm is drained even mid-calibration\n            readings.append(sensor.lux())\n            time.sleep(0.02)\n        if not readings:\n            readings = [sensor.lux()]\n        lo = int(min(readings))\n        hi = int(max(readings))\n        avg = int(sum(readings) / len(readings))\n        return \"OK|LCAL|min=%d|max=%d|avg=%d\" % (lo, hi, avg)\n    if line.startswith(\"WLUX|\"):\n        if sensor is None:\n            return \"ERR|NOSENSOR|WLUX\"\n        a = ints(line.split(\"|\")[1].split(\",\"), 5)\n        got = wait_range(a[0], a[1], a[2] or 2000, a[3] or 20000, a[4])\n        if got is None:\n            return \"ERR|TIMEOUT|WLUX\"\n        return \"OK|WLUX|lux=%d\" % int(got)\n    if line.startswith(\"TRGLUX|\"):\n        if sensor is None:\n            return \"ERR|NOSENSOR|TRGLUX\"\n        a = ints(line.split(\"|\")[1].split(\",\"), 10)\n        got = wait_range(a[0], a[1], a[2] or 2000, a[3] or 20000, a[4])\n        if got is None:\n            return \"ERR|TIMEOUT|TRGLUX\"\n        time.sleep(max(0, a[6]) / 1000)   # reaction delay (reactMin)\n        press(a[5], a[8] or 40)           # board-side keypress (holdMin)\n        return \"EVT|TRGLUX|lux=%d|vk=%d\" % (int(got), a[5])\n    if line in (\"HALT\", \"BYE\"):\n        if arm is not None:\n            forward_to_arm(line, 2)          # stop the arm too\n        return \"OK|\" + line\n    # v0.9.60 - final contract: the Pico ALWAYS owns the keyboard. Both envelopes are\n    # consumed locally; the legacy arm envelope is accepted for backward compatibility\n    # but is never forwarded (the Pro Micro never types).\n    if line.startswith(\"KBDPICO|\"):\n        line = line.split(\"|\", 1)[1]\n    elif line.startswith(\"KBDARM|\"):\n        line = line.split(\"|\", 1)[1]\n    head = line.split(\"|\")[0]\n    if head in KBD_PREFIXES:\n        return handle_keyboard(line, head)\n    if head in MOUSE_PREFIXES:\n        return forward_fast(line)\n    if head in ARM_PREFIXES:\n        tmo = 30 if head in (\"WSND\", \"TRGSND\", \"SCAL\") else 5\n        return forward_to_arm(line, tmo)\n    return \"ERR|UNKNOWN|\" + line\n\n\ndef standalone_pass():\n    # No host command pending: run the armed states of this system in order.\n    if sensor is None:\n        return                                # v0.9.60 - light states stay silent without a sensor\n    for st in states:\n        if not st.get(\"armed\"):\n            continue\n        got = wait_range(st.get(\"luxLow\", 0), st.get(\"luxHigh\", 0),\n                         st.get(\"stableMs\", 2000), st.get(\"timeoutMs\", 20000),\n                         st.get(\"mode\", 0))\n        if got is not None:\n            press(st.get(\"keyVk\", 69), 40)\n\n\ndef loop_due():\n    # Play Options baked at export: how often the armed states run standalone\n    if LOOP_MODE == \"once\":\n        return passes == 0\n    if LOOP_MODE == \"times\":\n        return passes < LOOP_COUNT\n    if LOOP_MODE == \"timed\":\n        return time.monotonic() - started < LOOP_SECONDS\n    return True                      # forever\n\n\nbuffer = bytearray()               # v0.9.60 - bounded byte buffer, not string concat\npasses = 0\nstarted = time.monotonic()\nwhile True:\n    try:\n        pump_arm()                            # v0.9.60 - the arm is drained EVERY iteration\n        if serial is not None and serial.in_waiting:\n            buffer.extend(serial.read(serial.in_waiting))\n            if len(buffer) > 4096:            # runaway guard: keep the newest 1 KB\n                del buffer[:-1024]\n            while True:\n                nl = buffer.find(b\"\\n\")\n                if nl < 0:\n                    break\n                raw = bytes(buffer[:nl])\n                del buffer[:nl + 1]\n                line = raw.decode(\"utf-8\", \"replace\").strip()\n                if not line:\n                    continue\n                last_host_cmd = time.monotonic()\n                try:\n                    _serial_write_line(handle(line))\n                except Exception:\n                    _serial_write_line(\"ERR|EXC|\" + line.split(\"|\")[0])\n        else:\n            poll_keypad()\n            host_quiet = time.monotonic() - last_host_cmd >= 3   # no double-fire after host runs\n            if engine_on and not engine_paused and host_quiet and states and loop_due():\n                standalone_pass()\n                passes += 1\n            time.sleep(0.02)\n    except Exception:\n        # v0.9.60 - never-die: a bad line or a transient USB hiccup must never kill\n        # code.py. (KeyboardInterrupt/Ctrl+C still stops it - it is not an Exception.)\n        try:\n            time.sleep(0.05)\n        except Exception:\n            pass\n"


def main():
    if not (ROOT / "ams-shell").is_dir() or not (ROOT / "tests").is_dir():
        print("ERROR: run from the repo root (folder containing ams-shell/ and tests/)")
        sys.exit(2)

    for rel, mode, old, new, expect_min in EDITS:
        edit(rel, mode, old, new, expect_min)
    swap_code_template("ams-shell/src/Ams.UI/Services/PicoFirmwareExporter.cs", CODE_REGION)
    write_file("project-state/PROJECT-STATE.md", PROJECT_STATE)
    append_file("MEMORY.md", "## ۲۰۲۶-۰۹-۰۸ — v0.9.60", MEMORY_APPEND)
    write_file("docs/firmware-consolidation-v0.9.60.md", DOC_NOTE)
    write_file("sim/sim60.py", SIM_SOURCE)                 # repo-mirror rule: the fake-hardware harness
    write_file("firmware/pico-light-0.9.60-template.py", FIRMWARE_TEMPLATE)   # canonical reference

    # mirror the patch itself into tools/ (repo-mirror rule)
    try:
        src = Path(__file__).resolve()
        dst = ROOT / "tools" / "patch-v0.9.60.py"
        if src != dst and (not dst.exists() or dst.read_bytes() != src.read_bytes()):
            shutil.copyfile(src, dst)
            REPORT.append("copied: tools/patch-v0.9.60.py")
    except Exception as ex:
        REPORT.append(f"note: self-copy to tools/ skipped ({ex})")

    # ── verify ──
    checks = [
        ("ams-shell/bridge/bridge.py", ['reconfigure(encoding="utf-8", errors="replace")', "ensure_ascii=True"]),
        ("ams-shell/src/Ams.UI/Services/PicoFirmwareExporter.cs",
         ["ERR|NOSENSOR", "AUTOSTART = False", "board.GP4", "board.GP3", "buffer = bytearray()",
          "def pump_arm()", "def forward_fast(", 'BundleVersion = "0.9.60"', "KEYPAD_NUMLOCK", "SCROLL_LOCK"]),
        ("ams-shell/src/Ams.UI/Services/PythonBoardBridge.cs", ["StandardOutputEncoding = Encoding.UTF8", "PYTHONIOENCODING"]),
        ("ams-shell/src/Ams.UI/MainWindow.xaml", ["SerialLogList", 'SelectionMode="Extended"']),
        ("ams-shell/src/Ams.UI/Views/StepDialog.xaml.cs", ["TryParseFloat", "CultureInfo.InvariantCulture"]),
        ("ams-shell/src/Ams.UI/Models/StepDefinitions.cs", ["StableSecText", "using System.Globalization;"]),
        ("ams-shell/src/Ams.UI/ViewModels/MainViewModel.cs", ["Classroom Studio v0.9.60"]),
        ("ams-shell/src/Ams.UI/Ams.UI.csproj", ["<Version>0.9.60</Version>"]),
        ("tests/TestRunner.cs", ["Step 58: v0.9.60", "var curMinor = 60;", "0.9.60"]),
        ("project-state/PROJECT-STATE.md", ["v0.9.60", "ci-16"]),
    ]
    for rel, needles in checks:
        try:
            text = (ROOT / rel).read_text(encoding="utf-8")
        except FileNotFoundError:
            FAILED.append(f"verify: missing {rel}")
            continue
        for nd in needles:
            if nd not in text:
                FAILED.append(f"verify: {nd!r} not found in {rel}")

    # the embedded firmware template must still compile after placeholder fill
    try:
        i = CODE_REGION.index('"""') + 3
        body = CODE_REGION[i:CODE_REGION.rindex('"""')]
        import textwrap
        tpl = textwrap.dedent(body)
        filled = (tpl.replace("__MACHINE__", "SELFTEST").replace("__GENERATED__", "now")
                     .replace("__VERSION__", "0.9.60").replace("__STATE_COUNT__", "0")
                     .replace("__LOOP_MODE__", "forever").replace("__LOOP_COUNT__", "0")
                     .replace("__LOOP_SECONDS__", "0"))
        compile(filled, "code.py", "exec")
        REPORT.append("self-test: filled firmware template compiles (py_compile equivalent)")
    except Exception as ex:
        FAILED.append(f"self-test: firmware template does not compile: {ex}")

    print()
    for line in REPORT:
        print("  " + line)
    if FAILED:
        print()
        print("FAILURES:")
        for line in FAILED:
            print("  " + line)
        sys.exit(1)
    print()
    print("v0.9.60 patch applied cleanly.")


if __name__ == "__main__":
    main()
