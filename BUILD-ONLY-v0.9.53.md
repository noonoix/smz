# BUILD-ONLY — v0.9.53

فقط بیلد + تست + پکیج. هیچ تغییری در سورس لازم نیست.

```powershell
cd ams-shell\src\Ams.UI
dotnet build -c Release 2>&1 | Tee-Object ..\..\..\build-v0.9.53.txt
cd ..\..\..\tests
dotnet run -c Release 2>&1 | Tee-Object ..\test-output-v0.9.53.txt
```

## پذیرش

- `0 Error(s)` در بیلد (هشدار CS8629 منتطره و بی‌اشکال است)
- `=== Results: N passed, 0 failed ===` — این بار ۲۰ شکست نسخه‌ای قبلی هم اصلاح شده
- هر ۲۰ خط `v0.9.53:` با PASS
- `FileVersion` در DLL = `0.9.53.0`
- در زیپ خروجی: `caterina\`، `tools\isp_flash.py`، bridge.py با هش `e72530b2`، بدون هیچ `.cs`/`.xaml`

## تست دستی روی ویندوز

1. فقط COM17 را وصل کنید → در قدم ۴ فقط همین یک ردیف باید بدون نشانه‌ی تاریخچه باشد؛ اگر مودم/دانگل دیگری وصل است، آن هم باید زنده دیده شود (قبلاً تاریخچه برچسب می‌خورد).
2. دکمه‌ی پاکسازی را بزنید → در لاگ باید خطوط «حذف شد (pnputil)» یا «حذف شد (مالکیت رجیستری)» یا «حذف شد (اجرای SYSTEM)» دیده شود و شمارش پورت‌ها کم شود.
3. دیگر خط «رد شد (خارج از خانواده‌های مجاز)» برای بردهای خودمان (2341/1D50/04D8) نباید دیده شود.
4. اگر یک ورودی باز هم حذف نشد، خط FAIL دقیقاً می‌گوید هر سه روش شکست خورده — همان خط را بفرستید.
