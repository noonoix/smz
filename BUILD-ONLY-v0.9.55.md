# BUILD-ONLY - v0.9.55

فقط بیلد + تست + پکیج. هیچ تغییری در سورس لازم نیست.

```powershell
cd ams-shell\src\Ams.UI
dotnet build -c Release 2>&1 | Tee-Object ..\..\..\build-v0.9.55.txt
cd ..\..\..\tests
dotnet run -c Release 2>&1 | Tee-Object ..\test-output-v0.9.55.txt
```

## پذیرش

- `0 Error(s)` در بیلد
- `=== Results: N passed, 0 failed ===`
- همه‌ی خطوط `v0.9.55:` با PASS
- `FileVersion` در DLL = `0.9.55.0`

## تست دستی روی ویندوز

1. قدم ۱: بیست صفحه‌کلید تازه در لیست دستگاه‌ها دیده می‌شوند و انتخابشان فرم مشخصات برد را پر می‌کند.
2. تب تنظیمات: باید داخل همان پنجره‌ی اصلی به‌صورت پنل راست باز شود، نه پنجره‌ی شناور؛ هر چهار بخش کامل، تأیید و انصراف کار کند.
3. راهنمای شناور آیکون‌های ریل زیر آیکون ظاهر شود و وقتی زیرمنو باز است دیده نشود.
4. در دیالوگ استپ و تب‌های آماده‌سازی برد، نوار اسکرول از متن فارسی فاصله دارد.
5. روی سر یک مجموعه (مثلاً Parallel Group) دوبار کلیک کنید، «عنوان مجموعه» را پر کنید → سر مجموعه با نماد ⚡ و نام دلخواه نمایش داده شود.
6. ردیف‌های نشانگر باید `next` و `else` (بدون `#`) و چسبیده به رگ قرمز باشند.
