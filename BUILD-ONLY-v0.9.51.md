# BUILD-ONLY v0.9.51 — فقط build / test / publish (بدون هیچ تصمیم کدنویسی)

این بسته **سورس کامل v0.9.51** است (مبنا: v0.9.50c). کار تو فقط سه مرحله است. **هیچ فایلی
را ویرایش نکن، هیچ باگی را «فیکس» نکن.** اگر چیزی شکست خورد، متن کامل خطا را برگردان و توقف کن.

## چه چیزی تازه است (فقط برای اطلاع — دست‌کاری نکن)

- **تب چکاپ:** تفکیک فانتوم/زنده (ردیف تاریخچه با 🕘 نشان می‌خورد · HELLO فقط به زنده‌ها ·
  خلاصه فقط زنده‌ها را «آنلاین» می‌شمارد) + دکمه‌ی **🧹 پاکسازی تاریخچه‌ی COM…** که بعد از
  تأیید، همان exe را با `--com-cleanup` و اجازه‌ی ادمین دوباره اجرا می‌کند (بدون پنجره):
  بکاپ .reg ← حذف فقط ورودی‌های فانتومِ خانواده‌های شناخته‌شده ← آزادسازی بیت‌های ComDB.
  سرویس تازه: `Services/BoardCleanupService.cs` (usingهای صریح کامل — درس v0.9.50c).
- **تب نصب IDE:** کارت «مشخصات برد» بالای پیش‌نمایش برگشت (شناسه/نام/VID/PID/محصول/سازنده،
  همگام با تب ۱).
- **راست‌چین:** هر TextBlock فارسیِ دستورالعمل/نکته در پنجره راست‌چین شد + آیتم‌های فهرست
  خلاصه؛ محتوای فنی (لاگ/پیش‌نمایش/دستور) چپ‌چین می‌ماند.
- نسخه‌ها: بنر + csproj + BundleVersion = **0.9.51**. `bridge.py` دست‌نخورده (`e72530b2…`).

## مرحله ۱ — جایگزینی کامل درخت

قانون طلایی ۱: **هرگز selective merge نه.** درخت قبلی را کامل پاک کن و از زیپ دوباره بساز.

## مرحله ۲ — build و تست

```powershell
cd ams-shell
dotnet build -c Release
```

انتظار: **۰ خطا**. (دو warning شناخته‌شده‌ی `CS8629` روی VisionService.cs از قبل هستند.)

```powershell
cd ..\tests
dotnet run 2>&1 | Tee-Object test-output-v0.9.51.txt
```

**ملاک پذیرش:** خط آخر `0 failed` + هر **۱۶** خط PASS ِ گام ۵۱ (`v0.9.51: …`) حاضر باشد.
**عدد کل دقیق نیست** (درس v0.9.46)؛ حدوداً `522 ± 13` طبیعی است. در غیر این صورت خروجی کامل
را برگردان و توقف.

## مرحله ۳ — publish و ریلیز

```powershell
cd ..\ams-shell\src\Ams.UI
dotnet publish -c Release -r win-x64 --self-contained false
```

زیپ ریلیز: `Classroom-Studio-v0.9.51-release.zip` (همان ۱۵ فایل + `caterina\` و `tools\`).
**قبل از ارسال** تأیید کن:

1. `ClassroomStudio.dll` → **FileVersion = 0.9.51.0** (درس ریلیز جعلی v0.9.39).
2. ریلیز شامل `bridge.py` (هش `e72530b2…`)، `caterina\Caterina.hex` و `tools\isp_flash.py`
   باشد و **هیچ فایل `.cs`/`.xaml` نداشته باشد**.

ریلیز + `test-output-v0.9.51.txt` را برگردان.

## تست بصری v0.9.51 (موردبه‌مورد گزارش بده)

۱) اسکن: ردیف‌های تاریخچه 🕘 علامت دارند و شمارش «N وصل · M تاریخچه» چاپ می‌شود · ۲) چکاپ
کامل دیگر روی فانتوم‌ها ۲ ثانیه تلف نمی‌کند و «پروگرمر آنلاین» فقط زنده‌ها را می‌شمارد ·
۳) 🧹: dry-run فهرست فانتوم‌های خانواده‌ی ما را می‌دهد (ZyXEL/Samsung هرگز) ← تأیید ← UAC
یک‌باره ← لاگ حذف/بکاپ ← اسکن دوباره پورت‌های آزادشده را نشان می‌دهد (فایل
com-cleanup-backup-*.reg کنار exe) · ۴) تب ۲: کارت «مشخصات برد» بالای پیش‌نمایش است و با
عوض‌کردن هویت در تب ۱ عوض می‌شود · ۵) همه‌ی نکات/دستورالعمل‌های فارسی راست‌چین‌اند.

## مشخصات بسته

- sha256 زیپ سورس: در پیام همراه ثبت شده است.
- بنر مورد انتظار در لاگ اپ: `Classroom Studio v0.9.51 — board checkup tells live ports from history…`
