{
  "version": 1,
  "baseline": "pico-light 0.9.64f",
  "edits": [
    {
      "old": "import busio\nimport usb_cdc\nimport usb_hid\nfrom adafruit_hid.keyboard import Keyboard\nfrom adafruit_hid.keycode import Keycode\n\n",
      "new": "import busio\nimport usb_cdc\nimport usb_hid\nimport supervisor\nfrom adafruit_hid.keyboard import Keyboard\nfrom adafruit_hid.keycode import Keycode\n\n"
    },
    {
      "old": "}\n# modifiers too, so KCOMBO shortcuts (Ctrl/Shift/Alt/Win + key) work on the Pico\nMOD_VK = {\n    0xA0: Keycode.LEFT_SHIFT, 0xA1: Keycode.RIGHT_SHIFT,\n    0xA2: Keycode.LEFT_CONTROL, 0xA3: Keycode.RIGHT_CONTROL,\n    0xA4: Keycode.LEFT_ALT, 0xA5: Keycode.RIGHT_ALT,\n",
      "new": "}\n# modifiers too, so KCOMBO shortcuts (Ctrl/Shift/Alt/Win + key) work on the Pico\nMOD_VK = {\n    # Generic Windows modifier VKs are emitted by PLAN|2/restart_windows.\n    0x10: Keycode.LEFT_SHIFT, 0x11: Keycode.LEFT_CONTROL, 0x12: Keycode.LEFT_ALT,\n    0xA0: Keycode.LEFT_SHIFT, 0xA1: Keycode.RIGHT_SHIFT,\n    0xA2: Keycode.LEFT_CONTROL, 0xA3: Keycode.RIGHT_CONTROL,\n    0xA4: Keycode.LEFT_ALT, 0xA5: Keycode.RIGHT_ALT,\n"
    },
    {
      "old": "states = load_states()\nwindow = []\n_lux_seq = 0       # read-only LUX? sequence; resets on every Pico boot\n\n# PLAN2 hotfix h1 - optional engine, but import failures must never be silent.\n_pe_import_error = None\ntry:\n    import plan_engine as _pe\nexcept Exception as exc:\n    _pe = None\n    _pe_import_error = exc\n    print(\"plan: engine import failed:\", exc)\n\n# v0.9.60 - the light sensor is OPTIONAL. A loose SDA/SCL wire must never kill the brain\n# before its command loop (that was the silent boot death): without the sensor the light\n",
      "new": "states = load_states()\nwindow = []\n_lux_seq = 0       # read-only LUX? sequence; resets on every Pico boot\n\n# PLAN2 hotfix h1 + AUTO_CYCLE_PATCH_0967_H6.\n_pe_import_error = None\n_cycle_import_error = None\ntry:\n    import plan_engine as _pe\n    import plan_cycle as _pc\n    from cycle_runtime import ResumeArmStore\n    from auto_resume_boot import AutoResumeBoot\n    import resume_essentials_runtime as _rer\nexcept Exception as exc:\n    _pe = None\n    _pc = None\n    _rer = None\n    _pe_import_error = exc\n    _cycle_import_error = exc\n    print(\"cycle: import failed:\", exc)\n\n# v0.9.60 - the light sensor is OPTIONAL. A loose SDA/SCL wire must never kill the brain\n# before its command loop (that was the silent boot death): without the sensor the light\n"
    },
    {
      "old": "    btn2 = digitalio.DigitalInOut(board.GP3)\n    btn2.direction = digitalio.Direction.INPUT\n    btn2.pull = digitalio.Pull.UP\n    _last_btn1 = True\n    _last_btn2 = True\nexcept Exception:\n    btn1 = btn2 = None\n    _last_btn1 = _last_btn2 = True\n\n# v0.9.62 - onboard LED (GP25): headless state + fault signalling. Absent on Pico W\n# (its LED lives on the wifi chip) - then led stays None and every LED call no-ops.\n",
      "new": "    btn2 = digitalio.DigitalInOut(board.GP3)\n    btn2.direction = digitalio.Direction.INPUT\n    btn2.pull = digitalio.Pull.UP\n    # Seed from the actual active-low electrical state. This prevents a first\n    # press made before the first idle poll from being swallowed.\n    _last_btn1 = not btn1.value\n    _last_btn2 = not btn2.value\nexcept Exception:\n    btn1 = btn2 = None\n    _last_btn1 = _last_btn2 = False\n\n# v0.9.62 - onboard LED (GP25): headless state + fault signalling. Absent on Pico W\n# (its LED lives on the wifi chip) - then led stays None and every LED call no-ops.\n"
    },
    {
      "old": "engine_on = AUTOSTART        # GP4 toggles this (Num Lock = Start/Stop)\nengine_paused = False        # GP3 toggles this (Scroll Lock = Pause/Resume)\n_btn1_since = None           # v0.9.64b - GP4 press timestamp for the panic-hold gesture\nlast_host_cmd = time.monotonic()\n\n\n",
      "new": "engine_on = AUTOSTART        # GP4 toggles this (Num Lock = Start/Stop)\nengine_paused = False        # GP3 toggles this (Scroll Lock = Pause/Resume)\n_btn1_since = None           # v0.9.64b - GP4 press timestamp for the panic-hold gesture\n_btn1_last_press = -10.0      # AUTO_CYCLE_INPUT_FIX: reject switch bounce/repeated edges\n_btn2_last_press = -10.0\nlast_host_cmd = time.monotonic()\n\n\n"
    },
    {
      "old": "# not match is dropped by the arm with ERR|CKSUM and NEVER executed - and a dropped path\n# point is invisible, because the next point of the humanized path is 2-3 px away.\nARM_FRAMING = True     # set False only for an arm running fw <= 2.2\n_cksum_errors = 0      # v0.9.64c - corrupted lines the arm rejected since boot\n\n\n",
      "new": "# not match is dropped by the arm with ERR|CKSUM and NEVER executed - and a dropped path\n# point is invisible, because the next point of the humanized path is 2-3 px away.\nARM_FRAMING = True     # set False only for an arm running fw <= 2.2\n# fw 0.9.67: Pro Micro 2.6 reports the host USB/HID lifecycle on private UART.\n_arm_host_usb_state = None     # \"UP\", \"SUSPEND\", or \"DOWN\"\n_arm_host_usb_seen = False\n_cksum_errors = 0      # v0.9.64c - corrupted lines the arm rejected since boot\n\n\n"
    },
    {
      "old": "    Arm EVT| lines stream to the PC live; fire-acked mouse OKs are discarded; every\n    other reply is returned for a waiting forward_to_arm.\"\"\"\n    global _arm_buf, _arm_lag, _pending_move, _arm_last_ack, _cksum_errors   # 60c: flow; 62: watchdog; 64c: cksum\n    global _noframe_errors                                                   # 64e: strict-framing rejects\n    if arm is None:\n        return []\n    try:\n",
      "new": "    Arm EVT| lines stream to the PC live; fire-acked mouse OKs are discarded; every\n    other reply is returned for a waiting forward_to_arm.\"\"\"\n    global _arm_buf, _arm_lag, _pending_move, _arm_last_ack, _cksum_errors   # 60c: flow; 62: watchdog; 64c: cksum\n    global _noframe_errors, _arm_host_usb_state, _arm_host_usb_seen                                                   # 64e: strict-framing rejects\n    if arm is None:\n        return []\n    try:\n"
    },
    {
      "old": "        line = raw.decode(\"utf-8\", \"replace\").strip()\n        if not line:\n            continue\n        if line.startswith(\"EVT|\"):\n            _serial_write_line(line)              # arm events reach the PC live\n            continue\n        if line.startswith(\"ERR|NOFRAME\"):\n            # v0.9.64e - arm fw 2.4 refused a line with no integrity frame. This is\n",
      "new": "        line = raw.decode(\"utf-8\", \"replace\").strip()\n        if not line:\n            continue\n        if line.startswith(\"EVT|HOSTUSB|\"):\n            state = line.rsplit(\"|\", 1)[-1]\n            if state in (\"UP\", \"SUSPEND\", \"DOWN\"):\n                changed = (not _arm_host_usb_seen or state != _arm_host_usb_state)\n                _arm_host_usb_state = state\n                _arm_host_usb_seen = True\n                if changed:\n                    print(\"cycle: arm host USB\", state)\n                    _serial_write_line(line)\n            continue\n        if line.startswith(\"EVT|\"):\n            _serial_write_line(line)              # other arm events reach the PC live\n            continue\n        if line.startswith(\"ERR|NOFRAME\"):\n            # v0.9.64e - arm fw 2.4 refused a line with no integrity frame. This is\n"
    },
    {
      "old": "    standalone engine (and sends Num Lock), GP3 toggles pause (and sends Scroll Lock).\n    v0.9.62 - a start edge re-arms via start_engine; the LED mirrors engine state.\"\"\"\n    global engine_on, engine_paused, _last_btn1, _last_btn2, _btn1_since\n    if btn1 is None:\n        return\n    b1 = not btn1.value\n    b2 = not btn2.value\n    if b1 and not _last_btn1:\n        _btn1_since = time.monotonic()   # v0.9.64b - measure the hold for the panic gesture\n        engine_on = not engine_on\n        if engine_on:\n            start_engine()            # v0.9.62 - a start always revives the run\n        else:\n            engine_paused = False\n            release_all_buttons()     # v0.9.64b - tracked-held only (a pointless MUP teleports the cursor)\n        tap_key(Keycode.KEYPAD_NUMLOCK)\n    if b2 and not _last_btn2:\n        engine_paused = not engine_paused\n        tap_key(Keycode.SCROLL_LOCK)\n    if not b1 and _last_btn1 and _btn1_since is not None:   # v0.9.64b - GP4 release edge\n        if time.monotonic() - _btn1_since >= 1.0:             # >= 1 s hold = panic release\n",
      "new": "    standalone engine (and sends Num Lock), GP3 toggles pause (and sends Scroll Lock).\n    v0.9.62 - a start edge re-arms via start_engine; the LED mirrors engine state.\"\"\"\n    global engine_on, engine_paused, _last_btn1, _last_btn2, _btn1_since\n    global _btn1_last_press, _btn2_last_press\n    if btn1 is None:\n        return\n    b1 = not btn1.value\n    b2 = not btn2.value\n    now = time.monotonic()\n    if b1 and not _last_btn1 and now - _btn1_last_press >= 0.30:\n        _btn1_last_press = now\n        _btn1_since = now              # measure the hold for the panic gesture\n        engine_on = not engine_on\n        if engine_on:\n            start_engine()\n            print(\"keypad: GP4 START accepted\")\n        else:\n            engine_paused = False\n            release_all_buttons()\n            print(\"keypad: GP4 STOP accepted\")\n        tap_key(Keycode.KEYPAD_NUMLOCK)\n    if b2 and not _last_btn2 and now - _btn2_last_press >= 0.30:\n        _btn2_last_press = now\n        engine_paused = not engine_paused\n        print(\"keypad: GP3 %s accepted\" % (\"PAUSE\" if engine_paused else \"RESUME\"))\n        tap_key(Keycode.SCROLL_LOCK)\n    if not b1 and _last_btn1 and _btn1_since is not None:   # v0.9.64b - GP4 release edge\n        if time.monotonic() - _btn1_since >= 1.0:             # >= 1 s hold = panic release\n"
    },
    {
      "old": "        press(a[5], a[8] or 40)           # board-side keypress (holdMin)\n        return \"EVT|TRGLUX|lux=%d|vk=%d\" % (int(got), a[5])\n    if line in (\"HALT\", \"BYE\"):\n        _flow_reset()                # v0.9.60e - stop wipes the mouse flow ledger\n        release_all_buttons(force=True)  # v0.9.64b - full shield on host HALT/BYE\n        if arm is not None:\n",
      "new": "        press(a[5], a[8] or 40)           # board-side keypress (holdMin)\n        return \"EVT|TRGLUX|lux=%d|vk=%d\" % (int(got), a[5])\n    if line in (\"HALT\", \"BYE\"):\n        try:\n            if _resume_store is not None:\n                _resume_store.clear()\n        except Exception:\n            pass\n        _flow_reset()                # v0.9.60e - stop wipes the mouse flow ledger\n        release_all_buttons(force=True)  # v0.9.64b - full shield on host HALT/BYE\n        if arm is not None:\n"
    },
    {
      "old": "            return False\n        raise RuntimeError(\"TRGSND failed: \" + reply)\n\n    def beep(self, freq, ms):\n        # PLAN|2 v3 buzzer contract. Import pwmio lazily to keep boot RAM low.\n        import pwmio\n        tone = None\n        try:\n            tone = pwmio.PWMOut(board.GP5, duty_cycle=0,\n                                frequency=int(freq), variable_frequency=True)\n            tone.duty_cycle = 32768\n            if not _plan_sleep_ms(int(ms)):\n",
      "new": "            return False\n        raise RuntimeError(\"TRGSND failed: \" + reply)\n\n    def release_all(self):\n        try:\n            kbd.release_all()\n        except Exception:\n            pass\n        release_all_buttons(force=True)\n\n    def halt(self):\n        self.release_all()\n\n    def beep(self, freq, ms):\n        # AUTO_CYCLE_PATCH_0967_H6: portable audio is fixed to GP6.\n        import pwmio\n        tone = None\n        try:\n            tone = pwmio.PWMOut(board.GP6, duty_cycle=0,\n                                frequency=int(freq), variable_frequency=True)\n            tone.duty_cycle = 32768\n            if not _plan_sleep_ms(int(ms)):\n"
    },
    {
      "old": "        try:\n            with open(PLAN_PATH, \"r\") as fh:\n                text = fh.read()\n            _plan_cache = _pe.parse_plan(text)\n            print(\"plan: loaded\", len(_plan_cache), \"ops\")   # v0.9.62 - confirm a good parse once per boot\n        except Exception as exc:\n            if isinstance(exc, OSError):\n                pass                                 # no plan.txt - normal bridge-only mode\n",
      "new": "        try:\n            with open(PLAN_PATH, \"r\") as fh:\n                text = fh.read()\n            _cycle_ops, _cycle_policy = _pc.parse_cycle_plan(text)\n            _plan_cache = text\n            print(\"plan: loaded\", len(_cycle_ops), \"ops\")   # AUTO_CYCLE_PATCH_0967_H6\n        except Exception as exc:\n            if isinstance(exc, OSError):\n                pass                                 # no plan.txt - normal bridge-only mode\n"
    },
    {
      "old": "    if not _plan_cache:\n        return False\n    try:\n        _pe.run_plan(_plan_cache, _PlanCtx())\n        return \"done\"                     # PLAN2_H6_CONTROL_FIX: finite plan completed\n    except _pe.PlanAbort:\n        release_all_buttons(force=True)   # v0.9.64b - full shield on the abort path\n        return \"abort\"\n",
      "new": "    if not _plan_cache:\n        return False\n    try:\n        _cycle_result = _pc.run_root(_plan_cache, _build_plan_ctx(), arm_store=_resume_store)\n        return \"done\" if _cycle_result == \"finished\" else _cycle_result\n    except _pe.PlanAbort:\n        release_all_buttons(force=True)   # v0.9.64b - full shield on the abort path\n        return \"abort\"\n"
    },
    {
      "old": "    return True                      # forever\n\n\nbuffer = bytearray()               # v0.9.60 - bounded byte buffer, not string concat\npasses = 0\nstarted = time.monotonic()\n",
      "new": "    return True                      # forever\n\n\n_resume_store = ResumeArmStore(\"/.auto_resume_armed\") if _pc is not None else None\n_resume_after_boot = False\n\ndef _usb_host_ready():\n    if _arm_host_usb_seen:\n        return _arm_host_usb_state == \"UP\"\n    try:\n        return bool(supervisor.runtime.usb_connected)\n    except Exception:\n        return False\n\ndef _usb_host_down():\n    if _arm_host_usb_seen:\n        return _arm_host_usb_state in (\"DOWN\", \"SUSPEND\")\n    try:\n        return not bool(supervisor.runtime.usb_connected)\n    except Exception:\n        return True\n\ndef _auto_start_root():\n    global engine_on, engine_paused, _resume_after_boot\n    _resume_after_boot = True\n    engine_on = True\n    engine_paused = False\n    start_engine()\n    print(\"cycle: AUTO_RESUME start\")\n    return True\n\nif _resume_store is not None:\n    _resume_boot = AutoResumeBoot(_resume_store, time.monotonic, _usb_host_ready,\n                                  _auto_start_root,\n                                  lambda: btn1 is not None and not btn1.value,\n                                  usb_down=_usb_host_down, stable_seconds=2)\n    try:\n        with open(PLAN_PATH, \"r\") as _cycle_fh:\n            _cycle_text = _cycle_fh.read()\n        _cycle_ops, _cycle_policy = _pc.parse_cycle_plan(_cycle_text)\n        if _cycle_policy is not None:\n            _resume_boot.configure(_cycle_policy[\"auto\"],\n                                   _cycle_policy[\"resume\"][0],\n                                   _cycle_policy[\"resume\"][1])\n    except Exception as _resume_error:\n        print(\"cycle: resume policy unavailable:\", _resume_error)\nelse:\n    class _NoResume:\n        def tick(self): return 0\n    _resume_boot = _NoResume()\n\n_essentials = _rer.Manager.from_file(\"/resume_essentials.txt\", now=time.monotonic) if _pc is not None else None\n\ndef _build_plan_ctx():\n    global _resume_after_boot\n    ctx = _PlanCtx()\n    ctx.plan_api = 3\n    if _essentials is not None:\n        _essentials.resume_pending = _resume_after_boot\n        ctx.resume_essentials = _essentials\n    _resume_after_boot = False\n    return ctx\n\nbuffer = bytearray()               # v0.9.60 - bounded byte buffer, not string concat\npasses = 0\nstarted = time.monotonic()\n"
    },
    {
      "old": "                except Exception:\n                    _serial_write_line(\"ERR|EXC|\" + line.split(\"|\")[0])\n        else:\n            poll_keypad()\n            host_quiet = time.monotonic() - last_host_cmd >= 3   # no double-fire after host runs\n            if engine_on and not engine_paused and host_quiet and loop_due():\n",
      "new": "                except Exception:\n                    _serial_write_line(\"ERR|EXC|\" + line.split(\"|\")[0])\n        else:\n            _resume_boot.tick()\n            poll_keypad()\n            host_quiet = time.monotonic() - last_host_cmd >= 3   # no double-fire after host runs\n            if engine_on and not engine_paused and host_quiet and loop_due():\n"
    },
    {
      "old": "                    if _plan_result == \"done\":\n",
      "new": "                    if _plan_result in (\"done\", \"expired\"):\n"
    },
    {
      "old": "                        release_all_buttons()\n                        tap_key(Keycode.KEYPAD_NUMLOCK)\n                elif states:\n",
      "new": "                        release_all_buttons()\n                        if _plan_result == \"done\":\n                            tap_key(Keycode.KEYPAD_NUMLOCK)\n                        else:\n                            print(\"cycle: restart armed; waiting for host reboot\")\n                elif states:\n"
    }
  ]
}
