# v0.9.0 — موتور HumanMouse + رفع باگ‌های سراسری

**تاریخ:** 2026-08-26 · **نسخه:** v0.9.0

---

## ۱. موتور حرکت انسانی موس (بازنویسی کامل Random Mouse Position)

نسخهٔ 0.8.5 یک منحنی دوقسمتی با یک نقطهٔ میانی می‌ساخت — نه مکث‌مداری داشت و نه واقعاً انسانی بود.
v0.9.0 یک موتور کامل معرفی می‌کند: `Services/HumanMouse.cs`

### مسیر: الگوریتم WindMouse
- جاذبه به‌سمت هدف + «باد» تصادفی + گام تطبیقی → ردّ متراکم و طبیعی (۱۰–۷۰ نقطه در هر حرکت)
- **رفع باگ مهم:** مسیر قبلی از گوشهٔ بالا-چپ *ریجن* شروع می‌شد، نه از موقعیت واقعی کرسر! حالا `Cursor.Position` مبدا است.
- پروفایل سرعت ease-in-out: شروع کند → اوج وسط → فرود نرم (وزن زنگوله‌ای 0.3 تا 2.4)
- **Overshoot & Correct:** با احتمال قابل‌تنظیم (پیش‌فرض ۱۵٪) اول کمی از هدف رد می‌شود، مکث کوتاه بازنشانی (۶۰–۱۸۰ms)، بعد تصحیح برمی‌گردد
- همهٔ نقاط به داخل صفحه کلمپ می‌شوند (باد می‌تواند چند پیکسل بیرون بزند)

### مدیریت مکث (درخواست کاربر — 0 تا 5 ثانیه)
چهار لایهٔ مکث، همه با بازهٔ تصادفی و همه از دیالوگ استپ قابل تنظیم:

| فیلد | پیش‌فرض | معنی |
|---|---|---|
| pauseBeforeMin/Max | 120–450 ms | مکث «فکر کردن» قبل از شروع حرکت |
| pauseAfterMin/Max | 150–600 ms | مکث نشستن بعد از رسیدن |
| midPauseChance + midPauseMin/Max | 12٪ · 100–400 ms | تردید وسط مسیر (حداکثر یک بار در هر حرکت) |
| idleEveryMin/Max + idlePauseMin/Max | هر ۵–۱۲ حرکت · 1000–5000 ms | **استراحت طولانی هر N حرکت** — همان «0 تا 5 ثانیه بین چند حرکت»؛ با max=0 خاموش می‌شود |

شمارندهٔ «هر N حرکت» روی RunEngine نگه داشته می‌شود → بین استپ‌ها و تکرارهای Play Options ادامه دارد.

### اعمال به بقیهٔ حرکت‌ها
- **Mouse Position** با تیک human → همان موتور با پرسیت Gentle (مکث‌های سبک، بدون استراحت طولانی)
- **Find Image** (moveAndClick / moveOnly / clickRestoreCursor) → حرکت انسانی به‌جای پرش مستقیم
- **Park Cursor** (چک‌باکس ATM در دیالوگ Find Image) که قبلاً ذخیره می‌شد ولی هرگز اجرا نمی‌شد، حالا کار می‌کند: بعد از اکشن، کرسر به گوشهٔ ریجن پارک می‌شود
- لاگ فقط یک خط خلاصه برای هر حرکت (`mouse: human move → (x,y) · N pts`) — نقاط میانی با `quiet: true` لاگ را سیل نمی‌کنند

### ScriptGenerator (خروجی PowerShell) — همان رفتار + سه باگ بحرانی قدیمی
1. **باگ parse:** بلاک قدیمی برای یک `if` شش `}` می‌گذاشت → اسکریپت تولیدی اصلاً parse نمی‌شد
2. **باگ runtime:** `[Math]::Random::Default.NextDouble()` وجود ندارد (نه در PS 5.1 نه هیچ‌جا) → crash. حالا `$script:rng = New-Object System.Random`
3. همان باگ مبدأ گوشهٔ ریجن → حالا `[System.Windows.Forms.Cursor]::Position`
4. کف سرعت ۱۵۰px/s — قبلاً draw از بازهٔ [0..2300] می‌توانست نزدیک صفر شود و یک حرکت ده‌ها ثانیه طول بکشد

---

## ۲. رفع باگ‌های دیگر

| # | باگ | فایل |
|---|---|---|
| 1 | playAudio غیر-لوپ: `finally` بلافاصله پلیر را Close می‌کرد → **صدا هیچ‌وقت پخش نمی‌شد** | RunEngine.cs |
| 2 | playAudio لوپ: بعد از Stop **تا ابد پخش می‌ماند**؛ حالا `StopAllAudio()` در پایان run | RunEngine.cs + MainViewModel.cs |
| 3 | `ReloadKeyBindings` با `bindings.Clear()` کلیدهای Ctrl+N/O/S، F2، Del، Ctrl+D، Alt+↑/↓ را **بی‌صدا می‌کشت**؛ حالا فقط ۴ بایندینگ پخش جایگزین می‌شوند | MainViewModel.cs |
| 4 | دکمهٔ Pause هرگز غیرفعال نمی‌شد (`IsNotPaused` notify نداشت) | MainViewModel.cs |
| 5 | Stop دکمهٔ Run را آزاد می‌کرد ولی انجین هنوز جمع نشده بود → امکان دو اجرای هم‌پوشان؛ حالا گیت `_runActive` | MainViewModel.cs |
| 6 | حلقهٔ Pause با `Task.Run(WaitOne)` هر ۳۰۰ms یک نخ می‌سوزاند → poll سادهٔ ۱۰۰ms | MainViewModel.cs |
| 7 | چک‌باکس «Do not activate the main window when stopped» هیچ‌جا استفاده نمی‌شد → حالا honored | MainViewModel.cs |
| 8 | **رگرسیون رویداد aborted:** bridge.py بعد از abort رویداد `aborted` می‌فرستد ولی C# کیس نداشت → SendAsync تا تایم‌اوت می‌چسبید و Run خاکستری می‌ماند | PythonBoardBridge.cs |
| 9 | مردن sidecar پایتون SendAsync/Connect معلق را هنگ می‌گذاشت → حالا fault می‌شود | PythonBoardBridge.cs |
| 10 | typeText حالت word-humanize فاصلهٔ بین کلمات را تایپ نمی‌کرد → «hello world» به «helloworld» می‌رسید | StepDefinitions.cs |
| 11 | Pause فقط *بین* استپ‌ها عمل می‌کرد؛ حالا `PausableDelay` → وسط تأخیرها و وسط حرکت موس هم فریز می‌شود | RunEngine.cs |
| 12 | تب‌های Options: Playback و Hotkeys می‌توانستند هم‌زمان باز بمانند → `SelectTab` واحد | OptionsDialog |
| 13 | رادیوهای «Repeat mode» در Options هیچ بایندینگ/هندلری نداشتند (UI مرده) → حذف شدند؛ تنظیم واقعی در پنل Play Options است | OptionsDialog.xaml |
| 14 | Cut/Copy/Paste فقط در منوی راست‌کلیک بود → به منوی Edit هم اضافه شد | MainWindow.xaml |

## ۳. تست‌ها
- `tests/TestRunner.cs` — Step 16: مسیر دقیق روی هدف، کلمپ صفحه، کادنس استراحت طولانی، کف سرعت، فاصلهٔ کلمات، براکت متوازن PS، نبود `[Math]::Random`
- `ams-shell/tests/windmouse_check.py` — پورت 1:1 الگوریتم: ۶۰۰ حرکت تصادفی، صفر شکست، میانگین ۳۲ نقطه/حرکت، ۴۶ استراحت طولانی در ۴۰۰ حرکت با فاصلهٔ ۵–۱۲

**نکته:** بیلد/تست روی ویندوز (`dotnet build` + TestRunner) انجام شود — این بسته روی لینوکس ویرایش شده و کامپایل C# در اینجا ممکن نیست.
