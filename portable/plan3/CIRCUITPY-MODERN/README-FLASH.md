# Classroom Studio modern split-memory Pico Guard bundle

Generated by Classroom Studio v0.9.67 for Raspberry Pi Pico with CircuitPython
10.3.0. This is the modern runtime used for physical debugging; Golden 100 stays
separate and unchanged.

## Required Pro Micro firmware

Flash `firmware/arm28/ams_board28.ino` first. ARM 2.8 adds genuine relative HID
movement (`MMOVE ... rel`) while preserving ARM 2.7 mouse, sound, framed UART, HOSTUSB and
security behavior. Relative movement starts at the real current Windows cursor;
it does not need Classroom Studio, GuardHardwareMonitor, a bridge, `CURSOR|x,y`,
or a centre reset.

Keep `ams_board28.ino`, `ams_board26_impl.h`, `human_mouse_v3.h`, and your private
`ams_key.h` in one Arduino sketch folder. Never publish the private key.

## Pico installation

1. Use Classroom Studio's second, modern-memory export button.
2. Export into an empty staging folder or copy directly to a freshly prepared
   `CIRCUITPY` root. Do not merge with Golden 100 or an older two-stage build.
3. Keep every exported file at the root of `CIRCUITPY`; do not add a nested folder.
4. Validate every entry in `SHA256SUMS.txt` after copying.
5. Do not install `adafruit_hid`; the runtime has its own compact keyboard driver.
6. Keep Guard off until the six light profiles and GP3/GP4 controls are verified.

## Wiring

- BH1750: `VCC -> 3V3`, `GND -> GND`, `SDA -> GP20`, `SCL -> GP21`, `ADDR -> GND`.
- GP4: short Start/Stop; long enter/exit calibration.
- GP3: Pause/Resume outside calibration; sample inside calibration.
- GP6: passive piezo.
- Pico UART0 to Pro Micro: GP16 TX, GP17 RX, 57600 8N1, common GND.

`resumable_steps.txt` remains archived/inactive. `targeted_steps.txt` remains an
existing route only; this change adds no new Targeted behavior.
