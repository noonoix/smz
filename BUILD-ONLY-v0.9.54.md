# BUILD-ONLY — v0.9.54

فقط بیلد + تست + پکیج. هیچ تغییری در سورس لازم نیست.

```powershell
cd ams-shell\src\Ams.UI
dotnet build -c Release 2>&1 | Tee-Object ..\..\..\build-v0.9.54.txt
cd ..\..\..\tests
dotnet run -c Release 2>&1 | Tee-Object ..\test-output-v0.9.54.txt
```

## پذیرش

- `0 Error(s)` در بیلد
- `=== Results: N passed, 0 failed ===`
- هر ۱۹ خط `v0.9.54:` با PASS
- `FileVersion` در DLL = `0.9.54.0`

## تست دستی روی ویندوز

1. قدم ۱: یک دستگاه از لیست انتخاب کنید → VID/PID/محصول/سازنده خودکار پر می‌شوند (دیگر خالی نیستند).
2. قدم ۲: هفت فیلد مشخصات برد پر و قابل ویرایشند؛ هر تغییر فوری در پیش‌نمایش boards.txt دیده می‌شود؛ دکمه‌ی بازنشانی همه را به پیش‌فرض برمی‌گرداند.
3. قدم ۴: با یک برد متصل، لیست باید فقط یک ردیف و وضعیت «۱ پورت وصل» نشان دهد؛ لاگ می‌گوید چند ورودی تاریخچه پنهان شد.
4. تیک «نمایش تاریخچه» را بزنید → ردیف‌های تاریخچه برمی‌گردند (بدون اسکن مجدد).
5. لیست دستگاه‌ها دیگر micro:bit / Calliope / Adafruit / ESP32-S2 / Raspberry Pi ندارد.
