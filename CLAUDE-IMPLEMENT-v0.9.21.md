# CLAUDE IMPLEMENT — v0.9.21

**موضوع:** رفع واقعی اجرای موازی — opهای فوری صفحه‌کلید + پیسی‌گذاری کاملاً سمت اپ
**تاریخ:** 2026-09-01 — **پایه:** v0.9.20 (بیلد 34)

---

## ۰. قانون طلایی

> **هرگز selective merge نکن.** کل `ams-shell/src/` را پاک کن و نسخه‌ی زیپ را کامل جایگزین کن.
> `tests/TestRunner.cs` و `docs/` را هم جایگزین کن.

> ⚠️ **هشدار فرایندی:** `test-output.txt` در بیلد 34 مربوط به ۲۴ اوت بود (56 تست) —
> یعنی گام‌های ۱۹ تا ۲۱ هرگز اجرا نشده‌اند. این بار اجرای TestRunner واقعاً لازم است.

---

## ۱. چرا v0.9.20 بدتر شد — اثبات از رکورد اجرا

رکورد اجرای واقعی نشان داد:

| معیار | کانفیگ کاربر | اندازه‌گیری‌شده در اجرا | علت |
|---|---|---|---|
| cadence کلید (میانه) | ~۱۹۰ms | **۴۰۸ms** | تاخیر دوبله: برد ~۱۹۰ + اپ ~۱۹۰ |
| فریز موس حین تایپ | ≤ ~۱s | **۸٫۶s و ۶٫۷s** | هر op صدها ms کانال تک‌سریال را نگه می‌داشت |
| تله‌پورت موس | ۰ | ۰ | ✅ لنگر v0.9.20 کار کرد |

ریشه: firmware حتی به KTEXT تک‌کاراکتری هم تاخیر per-key سمت‌برد اعمال می‌کند،
و bridge تا پاسخ نهایی op صبر می‌کند (قفل one-in-flight).

---

## ۲. رفع (در زیپ اعمال شده)

در `RunEngine.cs`، شاخه‌ی KTEXT در حالت موازی:

```csharp
// برای هر کاراکتر:
await Send("KTEXT|0,0," + c);                        // op فوری ~10ms
await PausableDelay(NextRandom(hMin, hMax), ct);     // cadence انسانی سمت اپ
```

- `ChunkKtextForParallel` هدر را به `KTEXT|0,0` بازنویسی می‌کند.
- هر op برد فقط ~۱۰ms کانال را نگه می‌دارد → موس بین کلیدها روان جریان می‌یابد.
- cadence تایپ = دقیقاً کانفیگ کاربر (نه دو برابر).

**پلن B اگر delay=0 را firmware floor کرد** (مثلاً تایپ هنوز تند به نظر رسید یا لاگ
خطا داد): به‌ازای هر کاراکتر `KDOWN|vk` / `KUP|vk` بفرست — هر دو op در README
«verified against firmware 1.6» هستند. به من گزارش بده تا پیاده‌اش کنم.

فایل‌های تغییریافته: `RunEngine.cs`، `tests/TestRunner.cs` (گام ۲۱ به‌روز شد)،
`MainViewModel.cs` (بنر)، `Ams.UI.csproj` (0.9.21)، مستندات.

---

## ۳. بلوک build — اجباری

```powershell
cd ams-shell
taskkill /F /IM ClassroomStudio.exe 2>$null
dotnet clean AMS.sln -c Release
dotnet build AMS.sln -c Release
dotnet run --project ..	ests\TestRunner.csproj
python tests\windmouse_check.py
```

خروجی TestRunner را در `test-output.txt` ذخیره کن تا تازگی آن قابل بررسی باشد.

---

## ۴. معیارهای پذیرش

- [ ] build بدون error و بدون warning جدید
- [ ] `TestRunner` — گام‌های ۱ تا ۲۱ همه PASS (از جمله گام ۲۱ با انتظارات `KTEXT|0,0`)
- [ ] بنر: `Classroom Studio v0.9.21 — instant keystroke ops ...`
- [ ] منوی **Insert** شامل **Parallel Group** است؛ هاتکی‌های تک‌کلیدی کار می‌کنند

### تست دستی — پروژه‌ی «move mouse in pj6»

1. همان parallelGroup با typeText + randomMousePosition را اجرا کن.
2. **سرعت تایپ:** باید همان cadence کانفیگ‌شده باشد (~۱۹۰ms میانه برای 80/300) —
   نه دو برابر کند. با رکوردر AMS قدیمی ضبط کن و میانه‌ی key cadence را بسنج.
3. **موس:** حین تایپ، موس در فواصل بین کلیدها می‌خزد — فریز چندثانیه‌ای نه.
   سکون‌های موس فقط همان مکث‌های انسانی کانفیگ‌شده‌ی خود استپ باشند.
4. **بدون پرش:** هیچ حرکت جدیدی از نقطه‌ی کهنه شروع نمی‌شود (لنگر v0.9.20).

اگر باز مشکل دیدی: رکورد خروجی را دوباره بفرست — همان‌طور که این بار با رکورد ریشه را
اثبات کردیم، دور بعدی را هم با داده می‌بندیم.
